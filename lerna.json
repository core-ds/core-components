{
    "packages": ["packages/*"],
    "npmClient": "yarn",
    "useWorkspaces": true,
    "version": "independent",
    "command": {
        "version": {
            "message": "chore: publish [skip ci]",
            "ignoreChanges": ["*.stories.*", "**/__snapshots__/**", "*.test.*", "**/codemod/**"]
        }
    }
}
