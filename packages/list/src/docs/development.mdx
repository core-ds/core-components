import { ArgsTable } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { List as ListTS } from '../Component';
import styles from '!!raw-loader!../components/item/index.module.css';

## Подключение

```jsx
import { List } from '@balafla/core-components/list';
```

## Свойства

<ArgsTable of={ListTS} />

## Кастомизация

Для более гибкой кастомизации, вместо дочернего элмента `listItem` можно передавать `typography`.
Это позволит задать элементу списка нужный цвет. Для `typography` используте view `primary-medium`.

## Переменные

<CssVars css={styles} />
