import { ArgsTabs } from 'storybook/blocks';
import { CalendarRange } from '../index';
import { CalendarRangePopover } from '../views/popover';
import { CalendarRangeStatic } from '../views/static';

## Подключение

```jsx
import { CalendarRange } from '@balafla/core-components/calendar-range';
```

## Использование dataTestId

В компоненте используется модификатор для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getCalendarRangeTestIds`.
Импорт из `@balafla/core-components/calendar-range/shared`.

Функция возвращает объект:

```jsx
{
    component: dataTestId,
    calendarContainerFrom: `${dataTestId}-container-from`,
    calendarContainerTo: `${dataTestId}-container-to`,
};
```

## Свойства

<ArgsTabs components={{CalendarRange, CalendarRangePopover, CalendarRangeStatic}} />
