import { Meta, Story, Props } from '@storybook/addon-docs';
import { select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { CalendarRange } from '@alfalab/core-components-calendar-range';

import { CalendarRange as CalendarRangeTS } from '../Component';
import { CalendarRangePopover as CalendarRangePopoverTS } from '../views/popover';
import { CalendarRangeStatic as CalendarRangeStaticTS } from '../views/static';

import { version } from '../../package.json';
import Changelog from '../../CHANGELOG.md';
import Description from './description.mdx';


<Meta
    title='Компоненты/CalendarRange'
    component={CalendarRange}
/>

<!-- Canvas -->

<Story name='CalendarRange'>
    <CalendarRange
        calendarPosition={select('calendarPosition', ['static', 'popover'], 'static')}
    />
</Story>

<!-- Docs -->

<ComponentHeader
    name='CalendarRange'
    version={version}
    package='@alfalab/core-components/calendar-range'
    stage={1}
/>

```jsx
import { CalendarRange } from '@alfalab/core-components/calendar-range';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props components={{ CalendarRange: CalendarRangeTS, CalendarRangePopover: CalendarRangePopoverTS, CalendarRangeStatic: CalendarRangeStaticTS }} />}
/>
