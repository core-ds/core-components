import { ArgsTabs, CssVars } from 'storybook/blocks';
import { PatternLock } from '../index';
import { PatternLockDesktop } from '../desktop';
import { PatternLockMobile } from '../mobile';
import vars from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { PatternLock } from '@balafla/core-components/pattern-lock';
import { PatternLockMobile } from '@balafla/core-components/pattern-lock/mobile';
import { PatternLockDesktop } from '@balafla/core-components/pattern-lock/desktop';
// Хук для очистки ввода
import { usePatternLockCleanup } from '@balafla/core-components/pattern-lock/shared';
// Также можно импортировать объект с состояниями темы
import { THEME_STATE } from '@balafla/core-components/pattern-lock';
```

## Использование dataTestId

В компоненте используются модификаторы для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getPatternLockTestIds`.
Импорт из `@balafla/core-components/pattern-lock/shared`.

Функция возвращает объект:

```jsx
{
    patternLock: dataTestId,
    forgotCodeBtn: `${dataTestId}-forgot-code-btn`,
    error: `${dataTestId}-error`,
    message: `${dataTestId}-message`,
};
```

## Свойства

<ArgsTabs components={{PatternLock, PatternLockDesktop, PatternLockMobile}} />

## Переменные

<CssVars css={vars} />
