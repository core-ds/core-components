import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { select, text } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { Status, colors } from '@alfalab/core-components-status';

import { Status as StatusTS } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';

<Meta title='Компоненты/Прочие компоненты/Status' component={Status} />

<!-- Canvas -->

<Story name='Status'>
    <Status
        color={select('color', colors, 'green')}
        view={select('view', ['soft', 'contrast'], 'soft')}
    >
        {text('children', 'Label')}
    </Status>
</Story>

<!-- Docs -->

<ComponentHeader
    name='Status'
    version={version}
    package='@alfalab/core-components/status'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=260%3A36153'
/>

```jsx
import { Status } from '@alfalab/core-components/status';
```

<Tabs description={<Description />} changelog={<Changelog />} props={<ArgsTable of={StatusTS} />} />
