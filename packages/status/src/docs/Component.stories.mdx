import { Meta, Story, ArgsTable, Markdown } from '@storybook/addon-docs';
import { select, text } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { Status, colors } from '@alfalab/core-components-status';

import { Status as StatusTS } from '../Component';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md?raw';

<Meta title='Components/Status' component={Status} id='Status' />

{/* Canvas */}

<Story name='Status'>
    <Status
        color={select('color', colors, 'green')}
        view={select('view', ['soft', 'contrast'], 'soft')}
    >
        {text('children', 'Label')}
    </Status>
</Story>

{/* Docs */}

<ComponentHeader
    name='Status'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?type=design&node-id=100845%3A2264&mode=design&t=Itgzojk9BOtPqYkS-1'
/>

```jsx
import { Status } from '@alfalab/core-components/status';
```

<Tabs
    description={<Description />}
    changelog={<Markdown>{Changelog}</Markdown>}
    props={<ArgsTable of={StatusTS} />}
/>
