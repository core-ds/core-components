import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { select, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { Button } from '@alfalab/core-components-button';

import { Calendar, CalendarMobile, PeriodSlider, usePeriod } from '../';
import { name, version } from '../../package.json';
import Changelog from '../../CHANGELOG.md';
import Description from './description.mdx';
import styles from '!!raw-loader!../index.module.css';
import vars from '!!raw-loader!../vars.css';


<Meta
    title='Компоненты/Calendar'
    component={Calendar}
/>


<!-- Canvas -->

<Story name='Calendar'>
    {React.createElement(() => {
        const offDays = [new Date().setDate(7)];
        const events = [
            new Date().setDate(3),
            new Date().setDate(17),
        ];
        const [value, setValue] = React.useState();
        return (
            <Calendar
                value={value}
                onChange={setValue}
                offDays={offDays}
                events={events}
                defaultView={select('defaultView', ['days', 'months', 'years'], 'days')}
                selectorView={select('selectorView', ['full', 'month-only'], 'full')}
            />
        );
    })}
</Story>

<Story name='CalendarMobile'>
    {React.createElement(() => {
        const [openFirst, setOpenFirst] = React.useState(false);
        const [openSecond, setOpenSecond] = React.useState(false);
        const [value, setValue] = React.useState();
        const { selectedFrom, selectedTo, updatePeriod } = usePeriod();
        return (
            <>
                <Button onClick={() => setOpenFirst(true)} style={{ margin: '15px' }}>
                    Открыть календарь
                </Button>
                <Button onClick={() => setOpenSecond(true)} style={{ margin: '15px' }}>
                    Открыть календарь (период)
                </Button>
                <CalendarMobile
                    onClose={() => setOpenFirst(false)}
                    open={openFirst}
                    value={value}
                    onChange={setValue}
                    defaultView={select('defaultView', ['days', 'months', 'years'], 'days')}
                    selectorView={select('selectorView', ['full', 'month-only'], 'full')}
                />
                <CalendarMobile
                    onClose={() => setOpenSecond(false)}
                    open={openSecond}
                    onChange={updatePeriod}
                    selectedFrom={selectedFrom}
                    selectedTo={selectedTo}
                    defaultView={select('defaultView', ['days', 'months', 'years'], 'days')}
                    selectorView={select('selectorView', ['full', 'month-only'], 'full')}
                />
            </>
        );
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='Calendar'
    version={version}
    package='@alfalab/core-components/calendar'
    stage={2}
/>

```tsx
import { Calendar } from '@alfalab/core-components/calendar';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props components={{ Calendar, PeriodSlider }} />}
    cssVars={<CssVars css={`${styles}${vars}`} />}
/>

