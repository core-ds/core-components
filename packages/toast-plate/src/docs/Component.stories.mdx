import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select, boolean } from '@storybook/addon-knobs';
import { Button } from '@alfalab/core-components-button';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { ToastPlate } from '@alfalab/core-components-toast-plate';

import { ToastPlate as ToastPlateTS } from '../component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../vars.css';

<Meta title='Компоненты/Уведомления/ToastPlate' component={ToastPlate} />

<!-- Canvas -->

<Story name='ToastPlate'>
    {React.createElement(() => {
        const colors = select('colors', ['default', 'inverted'], 'default');
        return (
            <div
                style={{
                    backgroundColor:
                        colors === 'inverted'
                            ? 'var(--color-light-bg-primary-inverted)'
                            : 'transparent',
                    padding: '10px',
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                }}
            >
                <ToastPlate
                    colors={colors}
                    badge={select(
                        'badge',
                        ['negative', 'positive', 'attention', undefined],
                        'positive',
                    )}
                    title={text('title', 'Toast Title')}
                    hasCloser={boolean('hasCloser', true)}
                    block={boolean('block', false)}
                    onClose={Function.prototype}
                    actionButton={
                        boolean('renderActionButton', false) ? (
                            <Button
                                view='ghost'
                                colors={colors === 'default' ? 'inverted' : 'default'}
                                size='s'
                            >
                                Action Button
                            </Button>
                        ) : null
                    }
                >
                    {text('children', '')}
                </ToastPlate>
            </div>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader name='ToastPlate' version={version} package={name} />

```jsx
import { ToastPlate } from '@alfalab/core-components/toast-plate';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={ToastPlateTS} />}
    cssVars={<CssVars css={styles} />}
/>
