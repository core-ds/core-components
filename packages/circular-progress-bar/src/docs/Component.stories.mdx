import { Meta, Story, Props } from '@storybook/addon-docs';
import { text, select, number, boolean } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { CircularProgressBar } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';


<Meta
    title='Компоненты/CircularProgressBar'
    component={CircularProgressBar}
/>


<!-- Canvas -->

<Story name='CircularProgressBar'>
    {React.createElement(() => {
        const value = number('value', 50);
        const view = select('view', ['positive', 'negative']);
        const completeTextColor = select('completeTextColor',['primary-inverted', 'positive', 'negative', 'secondary' ], 'secondary');
        const title = text('title', 'title');
        const titleComplete = text('titleComplete');
        const subtitle = text('subtitle', 'subtitle');
        const subtitleComplete = text('subtitleComplete');
        return (
            <React.Fragment>
                <CircularProgressBar
                    value={value}
                    view={view}
                    title={title}
                    titleComplete={titleComplete}
                    subtitle={subtitle}
                    subtitleComplete={subtitleComplete}
                    stroke={boolean('stroke', true)}
                    direction={boolean('direction', false)}
                    fillComplete={boolean('fillComplete', false)}
                    completeTextColor={completeTextColor}
                    size={select('size', ['xs', 's', 'm', 'l', 'xl', 'xxl'], 'l')}
                />
                <p>Без атрибута title значение для title берётся из value</p>
                <CircularProgressBar
                    value={value}
                    view={view}
                    subtitle={subtitle}
                    size='l'
                />
                <p>Если есть children, то они подставляются в центр, а title и subtitle игнорируются</p>
                <CircularProgressBar
                    value={value}
                    view={view}
                    title={title}
                    subtitle={subtitle}
                    size='l'
                >
                    <p>Hello</p>
                </CircularProgressBar>
            </React.Fragment>
        )
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='CircularProgressBar'
    version={version}
    package='@alfalab/core-components/circular-progress-bar'
    stage={2}
/>

```jsx
import { CircularProgressBar } from '@alfalab/core-components/circular-progress-bar';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props of={CircularProgressBar} />}
    cssVars={<CssVars css={styles} />}
/>
