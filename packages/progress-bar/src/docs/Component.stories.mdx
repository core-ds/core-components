import { Meta, Story, Props } from '@storybook/addon-docs';
import { select, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { ProgressBar } from '@alfalab/core-components-progress-bar';

import { ProgressBar as ProgressBarTS } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';


<Meta
    title='Компоненты/ProgressBar'
    component={ProgressBar}
/>


<!-- Canvas -->

<Story name='ProgressBar'>
    <ProgressBar
        value={number('value', 40)}
        view={select('view', ['positive', 'negative', 'attention', 'link', 'tertiary', 'secondary', 'primary', 'accent'])}
        size={select('size', ['s', 'm'], 'm')}
    />
</Story>


<!-- Docs -->

<ComponentHeader
    name='ProgressBar'
    version={version}
    package='@alfalab/core-components/progress-bar'
    stage={2}
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=781%3A30260'
/>

```jsx
import { ProgressBar } from '@alfalab/core-components/progress-bar';
```

<Tabs
    description={<Description />}
    props={<Props of={ProgressBarTS} />}
    cssVars={<CssVars css={styles} />}
    changelog={<Changelog />}
/>
