import { Meta, Story, ArgsTable, Markdown } from '@storybook/addon-docs';
import { select, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { ProgressBar } from '@alfalab/core-components-progress-bar';

import { ProgressBar as ProgressBarTS } from '../Component';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md?raw';
import styles from '!!raw-loader!../index.module.css';

<Meta title='Components/ProgressBar' component={ProgressBar} id='ProgressBar' />

{/* Canvas */}

<Story name='ProgressBar'>
    <ProgressBar
        value={number('value', 40)}
        view={select('view', [
            'positive',
            'negative',
            'attention',
            'link',
            'tertiary',
            'secondary',
            'primary',
            'accent',
        ])}
        size={select('size', ['s', 'm'], 'm')}
    />
</Story>

{/* Docs */}

<ComponentHeader
    name='ProgressBar'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?type=design&node-id=95706%3A121367&mode=design&t=Itgzojk9BOtPqYkS-1'
/>

```jsx
import { ProgressBar } from '@alfalab/core-components/progress-bar';
```

<Tabs
    description={<Description />}
    props={<ArgsTable of={ProgressBarTS} />}
    cssVars={<CssVars css={styles} />}
    changelog={<Markdown>{Changelog}</Markdown>}
/>
