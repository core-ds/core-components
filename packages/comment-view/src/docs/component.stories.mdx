import { Meta, Story, Props } from '@storybook/addon-docs';
import { text } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { CommentView } from '../Сomponent';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

<Meta
    title='Компоненты/CommentView'
    component={CommentView}
/>

<!-- Canvas -->
<Story name='CommentView'>
    {React.createElement(() => {
        const children = text('Текст комментария', 'Comment');
        return (
            <div>
                <CommentView>{children}</CommentView>
            </div>
        )
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='CommentView'
    version={version}
    package='@alfalab/core-components/comment-view'
    stage={2}
/>

```jsx
import { CommentView } from '@alfalab/core-components/comment-view';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props of={CommentView} />}
    cssVars={<CssVars css={styles} />}
/>
