import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select, boolean, number } from '@storybook/addon-knobs';
import { CssVars, ComponentHeader, Tabs } from 'storybook/blocks';
import { StarMIcon } from '@alfalab/icons-glyph/StarMIcon';

import { DateTimeInput } from '../';
import Changelog from '../../CHANGELOG.md';
import { version } from '../../package.json';
import Description from './description.mdx';
import styles from '!!raw-loader!../components/date-time-input/index.module.css';


<Meta
    title='Компоненты/DateTimeInput'
    component={DateTimeInput}
/>

<!-- Canvas -->

<Story name='DateTimeInput'>
    {React.createElement(() => {
        return (
            <DateTimeInput
                block={boolean('block', false)}
                size={select('size', ['s', 'm', 'l'], 's')}
                error={text('error', '')}
                hint={text('hint', '')}
                label={text('label', '')}
                picker={boolean('picker', false)}
                clear={boolean('clear', false)}
            />
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='DateTimeInput'
    version={version}
    package='@alfalab/core-components/date-time-input'
/>

```jsx
import { DateTimeInput } from '@alfalab/core-components/date-time-input';
import { DateTimeInputDesktop } from '@alfalab/core-components/date-time-input/desktop';
import { DateTimeInputMobile } from '@alfalab/core-components/date-time-input/mobile';
import { DateTimeInputResponsive } from '@alfalab/core-components/date-time-input/responsive';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={DateTimeInput} />}
    cssVars={<CssVars css={styles} />}
/>
