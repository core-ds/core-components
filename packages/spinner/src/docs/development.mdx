import { ArgsTabs, CssVars } from 'storybook/blocks';
import { Spinner, SpinnerPreset } from '../Component';
import vars from '!!raw-loader!../vars.css';

## Подключениe

```jsx
import { Spinner } from '@alfalab/core-components/spinner';
```

## Использование

```jsx
<Spinner size={20} lineWidth={2} {...restProps} />
```

или

```jsx
<Spinner.Preset preset={48} {...restProps} />
```

## Свойства

<ArgsTabs components={{ Spinner: Spinner, 'Spinner.Preset': SpinnerPreset }} />

## Переменные

<CssVars css={vars} />
