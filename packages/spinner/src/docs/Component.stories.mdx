import { Meta, Story, Markdown } from '@storybook/addon-docs';
import { boolean, select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { Spinner } from '@alfalab/core-components-spinner';

import Description from './description.mdx';
import Development from './development.mdx';
import Changelog from '../../CHANGELOG.md?raw';

<Meta title='Components/Spinner' component={Spinner} id='Spinner' />

{/* Canvas */}

<Story name='Spinner'>
    {React.createElement(() => {
        const colors = select('colors', ['default', 'inverted'], 'default');
        return (
            <div
                style={{
                    backgroundColor:
                        colors === 'inverted'
                            ? 'var(--color-light-bg-primary-inverted)'
                            : 'transparent',
                    padding: '8px',
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                }}
            >
                <Spinner
                    size={select('size', [16, 24, 48], 24)}
                    visible={boolean('visible', true)}
                    colors={colors}
                />
            </div>
        );
    })}
</Story>

{/* Docs */}

<ComponentHeader
    name='Spinner'
    children='Используется для индикации выполнения команды пользователя.'
/>

<Tabs
    description={<Description />}
    development={<Development />}
    changelog={<Markdown>{Changelog}</Markdown>}
/>
