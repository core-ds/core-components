import { Meta, Story } from '@storybook/addon-docs';
import { boolean, select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { Spinner } from '@alfalab/core-components-spinner';

import { name, version } from '../../package.json';
import Description from './description.mdx';
import Development from './development.mdx';
import Changelog from '../../CHANGELOG.md';

<Meta title='Компоненты/Прогресс/Spinner' component={Spinner} />

<!-- Canvas -->

<Story name='Spinner'>
    {React.createElement(() => {
        const colors = select('colors', ['default', 'inverted'], 'default');
        return (
            <div
                style={{
                    backgroundColor:
                        colors === 'inverted'
                            ? 'var(--color-light-bg-primary-inverted)'
                            : 'transparent',
                    padding: '8px',
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                }}
            >
                <Spinner
                    size={select('size', ['xs', 's', 'm'], 's')}
                    visible={boolean('visible', true)}
                    colors={colors}
                />
            </div>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='Spinner'
    version={version}
    package='@alfalab/core-components/spinner'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=8493%3A48194'
    children='Используется для индикации выполнения команды пользователя.'
/>

<Tabs
    description={<Description />}
    development={<Development />}
    changelog={<Changelog />}
/>
