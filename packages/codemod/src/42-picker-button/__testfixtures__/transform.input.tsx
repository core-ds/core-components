/* eslint-disable */
import React from 'react';

import { PickerButton, PickerButtonProps, getIcon } from "@alfalab/core-components/picker-button";

const noop = () => {};

export const Component = () => {
    return (
        <React.Fragment>
            <PickerButton options={[]}/>
        </React.Fragment>
    );
};
