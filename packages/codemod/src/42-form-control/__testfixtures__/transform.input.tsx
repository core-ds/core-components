/* eslint-disable */
import React from 'react';

import { FormControl, FormControlProps} from '@balafla/core-components/form-control';

const noop = () => {};

export const Component = () => {
    return (
        <React.Fragment>
            <FormControl />
        </React.Fragment>
    );
};
