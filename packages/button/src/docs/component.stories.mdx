import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { StarMIcon } from '@alfalab/icons-glyph/StarMIcon';
import { text, select, boolean } from '@storybook/addon-knobs';
import { CssVars, ComponentHeader, Tabs } from 'storybook/blocks';
import { Button } from '@alfalab/core-components-button';

import { Button as ButtonTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';
import colorsStyles from '!!raw-loader!../default.module.css';

export const VIEWS = ['primary', 'secondary', 'tertiary', 'link', 'ghost'];
export const SIZES = ['xxs', 'xs', 's', 'm', 'l', 'xl'];

<Meta title='Компоненты/Кнопки/Button' component={Button} />

<!-- Canvas -->

<Story name='Button'>
    {React.createElement(() => {
        const colors = select('colors', ['default', 'inverted'], 'default');
        return (
            <div
                style={{
                    backgroundColor:
                        colors === 'inverted'
                            ? 'var(--color-light-bg-primary-inverted)'
                            : 'transparent',
                    padding: '8px',
                    position: 'absolute',
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                }}
            >
                <Button
                    view={select('view', VIEWS, 'primary')}
                    colors={colors}
                    size={select('size', SIZES, 'm')}
                    href={text('href', '')}
                    loading={boolean('loading')}
                    disabled={boolean('disabled', false)}
                    block={boolean('block', false)}
                    nowrap={boolean('nowrap', false)}
                    leftAddons={boolean('leftAddons', false) && <StarMIcon />}
                    rightAddons={boolean('rightAddons', false) && <StarMIcon />}
                >
                    {text('label', 'Оплатить')}
                </Button>
            </div>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='Button'
    version={version}
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=61%3A73570'
/>

```jsx
import { Button } from '@alfalab/core-components/button';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={ButtonTS} />}
    cssVars={
        <CssVars
            css={`
                ${styles}${colorsStyles}
            `}
        />
    }
/>
