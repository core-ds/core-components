## Icon

```jsx live mobileOnly mobileHeight={100}
render(() => {
    return (
        <div style={{ margin: '10px -20px -20px' }}>
            <TabBarPrivate
                items={[
                    { key: 'money', icon: <DiamondsMIcon />, label: 'Деньги' },
                    { key: 'payments', icon: <DiamondsMIcon />, label: 'Платежи' },
                    { key: 'history', icon: <DiamondsMIcon />, label: 'История' },
                ]}
                trailingAddon={
                    <TabBarPrivateTrailingIconButton icon={<DiamondsMIcon />} label='Поддержка' />
                }
            />
        </div>
    );
});
```

## Image

```jsx live mobileOnly mobileHeight={100}
const IconComponent = ({ style, children, ...restProps }) => (
    <div {...restProps} style={{ ...style, width: 48 }}>
        {children}
    </div>
);

render(() => {
    return (
        <div style={{ margin: '10px -20px -20px' }}>
            <TabBarPrivate
                items={[
                    { key: 'money', icon: <DiamondsMIcon />, label: 'Деньги' },
                    { key: 'payments', icon: <DiamondsMIcon />, label: 'Платежи' },
                    { key: 'history', icon: <DiamondsMIcon />, label: 'История' },
                ]}
                trailingAddon={
                    <TabBarPrivateTrailingIconButton
                        Icon={IconComponent}
                        icon={
                            <img
                                style={{
                                    width: 48,
                                    height: 56,
                                    left: '50%',
                                    bottom: 0,
                                    position: 'absolute',
                                    transform: 'translate(-50%, 0)',
                                    objectFit: 'cover',
                                    objectPosition: 'top left',
                                }}
                                src='/images/test.png'
                            />
                        }
                        label='Новый'
                    />
                }
            />
        </div>
    );
});
```

## Full Image

```jsx live mobileOnly mobileHeight={100}
const IconComponent = ({ style, children, ...restProps }) => (
    <div {...restProps} style={{ ...style, width: 48 }}>
        {children}
    </div>
);

render(() => {
    return (
        <div style={{ margin: '0 -20px -20px' }}>
            <TabBarPrivate
                items={[
                    { key: 'money', icon: <DiamondsMIcon />, label: 'Деньги' },
                    { key: 'payments', icon: <DiamondsMIcon />, label: 'Платежи' },
                    { key: 'history', icon: <DiamondsMIcon />, label: 'История' },
                ]}
                trailingAddon={
                    <TabBarPrivateTrailingButton style={{ width: 64 }}>
                        <img
                            style={{
                                width: '100%',
                                height: '100%',
                                objectFit: 'cover',
                                objectPosition: 'top left',
                            }}
                            src='/images/test.png'
                        />
                    </TabBarPrivateTrailingButton>
                }
            />
        </div>
    );
});
```
