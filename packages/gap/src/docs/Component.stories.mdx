import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select } from '@storybook/addon-knobs';
import { Typography } from '@alfalab/core-components-typography';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { Gap } from '@alfalab/core-components-gap';

import { Gap as GapTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

export const SIZES = [
    '3xs',
    '2xs',
    'xs',
    's',
    'm',
    'l',
    'xl',
    '2xl',
    '3xl',
    '4xl',
    '5xl',
    '6xl',
    '7xl',
    '8xl',
];

<Meta title='Компоненты/Gap' component={Gap} />

<Story name='Gap'>
    <Gap
        tag={select('size', SIZES, 's')}
        className={text('className', '')}
        dataTestId={text('dataTestId', '')}
    />
</Story>

<ComponentHeader name='Gap' version={version} package='@alfalab/core-components/Gap' />

```jsx
import { Gap } from '@alfalab/core-components/gap';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={GapTS} />}
    cssVars={<CssVars css={styles} />}
/>
