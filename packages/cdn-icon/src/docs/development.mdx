import { ArgsTable } from '@storybook/addon-docs';
import { Plate } from '@balafla/core-components/plate';
import { CDNIcon } from '../index';

## Подключение

```jsx
import { CDNIcon } from '@balafla/core-components/cdn-icon';
```

## Использование

Хранилище alfabank.servicecdn.ru — временное и в какой-то момент домен изменится.

<Plate
    view='attention'
    limitContentWidth={false}
    title='Рекомендуется использовать своё хранилище (baseUrl).'
    titleView='light'
/>

## Свойства

<ArgsTable of={CDNIcon} />
