import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select, boolean, number } from '@storybook/addon-knobs';
import { CssVars, ComponentHeader, Tabs } from 'storybook/blocks';
import { StarMIcon } from '@alfalab/icons-glyph/StarMIcon';

import { DateRangeInput } from '../Component';
import Changelog from '../../CHANGELOG.md';
import { version } from '../../package.json';
import Description from './description.mdx';
import styles from '!!raw-loader!../index.module.css';


<Meta
    title='Компоненты/DateRangeInput'
    component={DateRangeInput}
/>

<!-- Canvas -->

<Story name='DateRangeInput'>
    {React.createElement(() => {
        return (
            <DateRangeInput
                block={boolean('block', false)}
                size={select('size', ['s', 'm', 'l'], 's')}
                error={text('error', '')}
                hint={text('hint', '')}
                label={text('label', '')}
                picker={boolean('picker', false)}
                clear={boolean('clear', false)}
            />
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='DateRangeInput'
    version={version}
    package='@alfalab/core-components/date-range-input'
/>

```jsx
import { DateRangeInput } from '@alfalab/core-components/date-range-input';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={DateRangeInput} />}
    cssVars={<CssVars css={styles} />}
/>
