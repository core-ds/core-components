import { ArgsTable } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { Amount } from '../index';
import styles from '!!raw-loader!../index.module.css';

## Подключение

```jsx
// Компаунд импорт
import { Amount } from '@balafla/core-components/amount';

// Атомарные импорты
import { AmountComponent, Pure } from '@balafla/core-components/amount';
```

## Свойства

<ArgsTable of={Amount} />

## Переменные

<CssVars css={styles} />
