import { ArgsTable, Meta, Story } from '@storybook/addon-docs';
import { ComponentHeader } from 'storybook/blocks';
import { Markdown } from '../Component';
import packageJson from '../../package.json';
import { Tabs } from 'storybook/blocks';
import Description from './description.mdx';
import { number, radios, text } from '@storybook/addon-knobs';

<Meta title='Components/Markdown' component={Markdown} id='Markdown' />

<Story name='Markdown'>
    {React.createElement(() => {
        const paddings = {
            paddingTop: number("paddingTop", null),
            paddingBottom: number("paddingBottom", null),
            paddingLeft: number("paddingLeft", null),
            paddingRight: number("paddingRight", null)
        }
        return <Markdown paddings={paddings} font={radios("font", ["styrene", "system"], "styrene")} platform={radios("platform", ["desktop", "mobile"], "desktop")}>{text("children",
            `
### h1

## h2

hello

1. 123

2. 333
   `
   )}</Markdown>;
   })}
      </Story>

<ComponentHeader
    name='Markdown'
    version={packageJson.version}
    package='@alfalab/core-components/markdown'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/branch/iXBg8h9LGQzDyIJT0OHM4v/Web-%3A%3A-Core-Default-Components?type=design&node-id=89835-120143&mode=design&t=lrmb1JJ6HkunHzwe-0'
    children='Компонент для создания разметки.'
/>

## Подключение

```jsx
import { Markdown } from '@alfalab/core-components/markdown';
```

<Tabs description={<Description />} props={<ArgsTable of={Markdown} />} />
