import { ArgsTabs, CssVars } from 'storybook/blocks';
import { CheckboxGroup } from '../Component.responsive';
import { CheckboxGroupMobile } from '../mobile';
import { CheckboxGroupDesktop } from '../desktop';
import styles from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { CheckboxGroup } from '@balafla/core-components/checkbox-group';
import { CheckboxGroupMobile } from '@balafla/core-components/checkbox-group/mobile';
import { CheckboxGroupDesktop } from '@balafla/core-components/checkbox-group/desktop';
```

Из индекса импортируется responsive версия компонента.

## Свойства

<ArgsTabs components={{ CheckboxGroupDesktop, CheckboxGroupMobile, CheckboxGroup }} />

## Переменные

<CssVars css={styles} />
