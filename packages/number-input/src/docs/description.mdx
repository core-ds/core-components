
```jsx live
render(() => {
    const [value, setValue] = React.useState('+11234,567');

    return (
        <div style={{ width: document.body.clientWidth < 450 ? '100%' : 320 }}>
            <NumberInput
                value={value}
                block={true}
                onChange={(e) => setValue(e.target.value)}
                label='Label'
                size='m'
                clear={true}
                onClear={() => setValue('')}
            />
        </div>
    );
});
```

