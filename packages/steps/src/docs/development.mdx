import { ArgTypes } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { Steps } from '../index';
import styles from '!!raw-loader!../components/step/index.module.css';

## Подключение

```jsx
import { Steps } from '@balafla/core-components/steps';
```

## Использование dataTestId

В компоненте используется модификатор для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getStepsTestIds`.
Импорт из `@balafla/core-components/steps/shared`.

Функция возвращает объект:

```jsx
{
    steps: dataTestId,
    step: `${dataTestId}-step`,
};
```

## Свойства

<ArgTypes of={Steps} />

## Переменные

<CssVars css={styles} />
