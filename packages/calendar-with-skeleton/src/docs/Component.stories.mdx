import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { ComponentHeader, Tabs } from 'storybook/blocks';

import { Button } from '@alfalab/core-components-button';
import { CalendarWithSkeleton } from '@alfalab/core-components-calendar-with-skeleton';

import { CalendarWithSkeleton as CalendarWithSkeletonTS } from '../Component';
import { version } from '../../package.json';
import Changelog from '../../CHANGELOG.md';
import Description from './description.mdx';

<Meta title='Components/Forms/Calendars/CalendarWithSkeleton' component={CalendarWithSkeleton} />

<!-- Canvas -->

<Story name='CalendarWithSkeleton'>
    {React.createElement(() => {
        const [visible, setVisible] = React.useState(false);
        return (
            <>
                <CalendarWithSkeleton calendarVisible={visible} />
                <Button size='xs' onClick={() => setVisible(!visible)}>
                    toggle
                </Button>
            </>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='CalendarWithSkeleton'
    version={version}
    package='@alfalab/core-components/calendar-with-skeleton'
/>

```jsx
import { CalendarWithSkeleton } from '@alfalab/core-components/calendar-with-skeleton';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={CalendarWithSkeletonTS} />}
/>
