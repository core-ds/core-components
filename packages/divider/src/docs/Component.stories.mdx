import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { Divider } from '@alfalab/core-components-divider';

import { Divider as DividerTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

<Meta title='Компоненты/Прочие компоненты/Divider' component={Divider} />

<!-- Canvas -->

<Story name='Divider'>
    <div>
        <div style={{ height: 24, margin: '8px 0', backgroundColor: '#eeeff1' }} />
        <Divider />
        <div style={{ height: 24, margin: '8px 0', backgroundColor: '#eeeff1' }} />
    </div>
</Story>

<!-- Docs -->

<ComponentHeader name='Divider' version={version} package='@alfalab/core-components/divider' />

```jsx
import { Divider } from '@alfalab/core-components/divider';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={DividerTS} />}
    cssVars={<CssVars css={styles} />}
/>
