import { Meta, Props } from '@storybook/addon-docs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { KeyboardFocusable } from '@alfalab/core-components-keyboard-focusable';

import { KeyboardFocusable as KeyboardFocusableTS } from '../Component';
import { name, version } from '../../package.json';
import Changelog from '../../CHANGELOG.md';
import Description from './description.mdx';


<Meta
    title='Компоненты/KeyboardFocusable'
    component={KeyboardFocusable}
    parameters={{ previewTabs: { canvas: { hidden: true }} }}
/>


<!-- Docs -->

<ComponentHeader
    name='KeyboardFocusable'
    version={version}
    package='@alfalab/core-components/keyboard-focusable'
    stage={3}
/>

```jsx
import { KeyboardFocusable } from '@alfalab/core-components/keyboard-focusable';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props of={KeyboardFocusableTS} />}
/>
