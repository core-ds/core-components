import { Meta, ArgsTable, Story, Markdown } from '@storybook/addon-docs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { KeyboardFocusable } from '@alfalab/core-components-keyboard-focusable';

import { KeyboardFocusable as KeyboardFocusableTS } from '../Component';
import Changelog from '../../CHANGELOG.md?raw';
import Description from './description.mdx';

<Meta title='Components/KeyboardFocusable' component={KeyboardFocusable} id='KeyboardFocusable' />

<Story name='KeyboardFocusable'>
    <></>
</Story>

{/* Docs */}

<ComponentHeader
    name='KeyboardFocusable'
/>

```jsx
import { KeyboardFocusable } from '@alfalab/core-components/keyboard-focusable';
```

<Tabs
    description={<Description />}
    changelog={<Markdown>{Changelog}</Markdown>}
    props={<ArgsTable of={KeyboardFocusableTS} />}
/>
