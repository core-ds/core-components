import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { boolean } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { Button } from '@alfalab/core-components-button';
import { Typography } from '@alfalab/core-components-typography';
import { Drawer, DrawerContext } from '@alfalab/core-components-drawer';

import { Drawer as DrawerTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

<Meta title=' Components/Modals&Overlays/Drawer' component={Drawer} />

<!-- Canvas -->

<Story name='Drawer'>
    {React.createElement(() => {
        const [open, setOpen] = React.useState(false);
        const handleModalOpen = () => setOpen(!open);
        return (
            <React.Fragment>
                <Button size='xs' onClick={handleModalOpen}>
                    Открыть Drawer
                </Button>
                <Drawer
                    open={open}
                    onClose={handleModalOpen}
                    keepMounted={boolean('keepMounted', false)}
                >
                    <Content />
                </Drawer>
            </React.Fragment>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader name='Drawer' version={version} package='@alfalab/core-components/drawer' />

```jsx
import { Drawer } from '@alfalab/core-components/drawer';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={DrawerTS} />}
    cssVars={<CssVars css={styles} />}
/>
