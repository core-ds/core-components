import { CssVars } from 'storybook/blocks';
import colorsBluetint from '!!raw-loader!../colors-bluetint.css';
import colorsAddons from '!!raw-loader!../colors-addons.css';
import colorsDecorative from '!!raw-loader!../colors-decorative.css';
import colorsQualitative from '!!raw-loader!../colors-qualitative.css';
import colorsSequential from '!!raw-loader!../colors-sequential.css';
import colorsPfm from '!!raw-loader!../colors-pfm.css';
import borderRadius from '!!raw-loader!../border-radius.css';
import gaps from '!!raw-loader!../gaps.css';
import shadowsBluetint from '!!raw-loader!../shadows-bluetint.css';
import typography from '!!raw-loader!../typography.css';
import mixins from '!!raw-loader!../mixins.css';

```css
@import '@balafla/core-components/vars';
```

Все CSS-переменные можно также импортировать в виде JS-переменных:

```typescript
import * as vars from '@balafla/core-components/vars';

vars.gap2xl === '32px'; //true
```

Для использования в проекте, добавьте один из [подготовленных бандлов](https://github.com/core-ds/core-components/tree/master/packages/vars/src/bundle) продуктов в один из корневых файлов CSS:

```css
/* app.css */
@import '@balafla/core-components/vars/bundle/click.css';
```

Теперь все переменные и миксины можно использовать в любом месте проекта:

```css
/* some-component.css */
.class {
    padding: var(--gap-16);
}
```

### Отдельные наборы переменных:

#### Отступы

```css
@import '@balafla/core-components/vars/gaps.css';
```

<CssVars css={gaps} expandable />
&nbsp;

#### Скругления

```css
@import '@balafla/core-components/vars/border-radius.css';
```

<CssVars css={borderRadius} expandable />
&nbsp;

#### Тени

```css
@import '@balafla/core-components/vars/shadows-bluetint.css';
```

<CssVars css={shadowsBluetint} expandable />
&nbsp;

#### Цвета bluetint

```css
@import '@balafla/core-components/vars/colors-bluetint.css';
```

<CssVars css={colorsBluetint} expandable />
&nbsp;

#### Цвета addons

```css
@import '@balafla/core-components/vars/colors-addons.css';
```

<CssVars css={colorsAddons} expandable />
&nbsp;

#### Цвета decorative

```css
@import '@balafla/core-components/vars/colors-decorative.css';
```

<CssVars css={colorsDecorative} expandable />
&nbsp;

#### Цвета qualitative

```css
@import '@balafla/core-components/vars/colors-qualitative.css';
```

<CssVars css={colorsQualitative} expandable />
&nbsp;

#### Цвета sequential

```css
@import '@balafla/core-components/vars/colors-sequential.css';
```

<CssVars css={colorsSequential} expandable />
&nbsp;

#### Цвета pfm

```css
@import '@balafla/core-components/vars/colors-pfm.css';
```

<CssVars css={colorsPfm} expandable />
&nbsp;

#### Переменные и миксины typography

```css
@import '@balafla/core-components/vars/typography.css';

//  Использование миксина
.class-name {
    @mixin headline_xlarge;
}
```

#### Переменные

#

#

<CssVars css={typography} expandable />
&nbsp;

#### Миксины

#

#

<CssVars css={typography} expandable type='mixins' />
&nbsp;

#### Дополнительные миксины

```css
@import '@balafla/core-components/vars/mixins.css';
```

<CssVars css={mixins} expandable type='mixins' />
&nbsp;
