import { ArgsTable } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { ActionButton } from '../index';
import defaultColorStyles from '!!raw-loader!../default.module.css';
import invertedColorStyles from '!!raw-loader!../inverted.module.css';

## Подключение

```jsx
import { ActionButton } from '@balafla/core-components/action-button';
```

## Использование dataTestId

В компоненте используется модификатор для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getActionButtonTestIds`.
Импорт из `@balafla/core-components/action-button/shared`.

Функция возвращает объект:

```jsx
{
    actionButton: dataTestId,
    spinner: `${dataTestId}-loader`,
};
```

## Свойства

<ArgsTable of={ActionButton} />

## Переменные

<CssVars
    css={`
        ${defaultColorStyles}${invertedColorStyles}
    `}
/>
