## Размеры

Компонент имеет несколько форм шейпа. Для каждой формы доступны следующие размеры:

```jsx live
render(() => {
    const SIZES = ['128', '80', '64', '48', '40', '32', '24', '20'];
    const [component, setComponent] = React.useState('SuperEllipse');

    const Component = {
        Circle,
        SuperEllipse,
        Rectangle,
        NoShape,
    }[component];

    const onChange = (_, payload) => {
        setComponent(payload.value);
    };

    const styles = {
        display: 'flex',
        justifyContent: 'center',
        gap: '16px',
    };

    return (
        <Container>
            <div style={styles}>
                {SIZES.map((size) => (
                    <Component size={size}>
                        {size === '128' || size === '80' ? (
                            <DiamondsXxlIcon />
                        ) : size === '20' ? (
                            <DiamondsSIcon />
                        ) : (
                            <DiamondsMIcon />
                        )}
                    </Component>
                ))}
            </div>
            <Row>
                <RadioGroup name='radioGroup' onChange={onChange} value={component}>
                    {['SuperEllipse', 'Circle', 'Rectangle', 'NoShape'].map((c) => (
                        <Radio
                            label={c === 'NoShape' ? 'noShape' : c.toLowerCase()}
                            value={c}
                            size='m'
                            key={c}
                        />
                    ))}
                </RadioGroup>
            </Row>
        </Container>
    );
});
//MOBILE
render(() => {
    const BIG_SIZES = ['128', '80', '64'];
    const SMALL_SIZES = ['48', '40', '32', '24', '20'];

    const [component, setComponent] = React.useState('SuperEllipse');

    const Component = {
        Circle,
        SuperEllipse,
        Rectangle,
        NoShape,
    }[component];

    const onChange = (_, payload) => {
        setComponent(payload.value);
    };

    const styles = {
        display: 'flex',
        justifyContent: 'center',
        gap: '16px',
    };

    return (
        <Container>
            <div style={styles}>
                {BIG_SIZES.map((size) => (
                    <Component size={size}>
                        {size !== '64' ? <DiamondsXxlIcon /> : <DiamondsMIcon />}
                    </Component>
                ))}
            </div>
            <Gap size='xl' />
            <div style={styles}>
                {SMALL_SIZES.map((size) => (
                    <Component size={size}>
                        {size !== '20' ? <DiamondsMIcon /> : <DiamondsSIcon />}
                    </Component>
                ))}
            </div>

            <Row>
                <RadioGroup name='radioGroup' onChange={onChange} value={component}>
                    {['SuperEllipse', 'Circle', 'Rectangle', 'NoShape'].map((c) => (
                        <Radio
                            label={c === 'NoShape' ? 'noShape' : c.toLowerCase()}
                            value={c}
                            size='m'
                            key={c}
                        />
                    ))}
                </RadioGroup>
            </Row>
        </Container>
    );
});
```

## Шейп и фоновый контент

На фоновый слой можно передать различные виды контента или их комбинацию. Шейп отсечёт контент по своей форме.

```jsx live
render(() => {
    const [component, setComponent] = React.useState('SuperEllipse');
    const [value, setValue] = React.useState({
        background: true,
        image: false,
        text: false,
        icon: false,
    });

    const Component = {
        Circle,
        SuperEllipse,
        Rectangle,
        NoShape,
    }[component];

    const onChangeRadio = (_, payload) => {
        setComponent(payload.value);
    };

    const onChange = (_, payload) => {
        setValue({ ...value, [payload.name]: payload.checked });
    };

    return (
        <Container>
            <Row>
                <Col>
                    <Component
                        size='80'
                        backgroundColor={
                            value.background ? 'var(--color-light-bg-secondary)' : 'transparent'
                        }
                        imageUrl={value.image && './images/imgBg.png'}
                        text={
                            value.text && (
                                <Typography.Title
                                    view='small'
                                    weight='bold'
                                    font='system'
                                    color='secondary'
                                >
                                    БК
                                </Typography.Title>
                            )
                        }
                    >
                        {value.icon && <DiamondsMIcon />}
                    </Component>
                </Col>
            </Row>

            <Row>
                <CheckboxGroup label='Контент' onChange={onChange}>
                    <Checkbox
                        label='Цвет фона'
                        name='background'
                        checked={value.background}
                        size='m'
                    />
                    <Checkbox label='Изображение' name='image' checked={value.image} size='m' />
                    <Checkbox label='Текст' name='text' checked={value.text} size='m' />
                    <Checkbox label='Иконка' name='icon' checked={value.icon} size='m' />
                </CheckboxGroup>
            </Row>

            <Row>
                <RadioGroup
                    name='radioGroup'
                    onChange={onChangeRadio}
                    label='Форма шейпа'
                    value={component}
                >
                    {['SuperEllipse', 'Circle', 'Rectangle', 'NoShape'].map((c) => (
                        <Radio
                            label={c === 'NoShape' ? 'noShape' : c.toLowerCase()}
                            value={c}
                            size='m'
                            key={c}
                        />
                    ))}
                </RadioGroup>
            </Row>
        </Container>
    );
});
```

## Бордер

Для большего контраста можно добавить шейпу бордер. Для значения `shape=noShape` бордер недоступен.

```jsx live
render(() => {
    const [border, setBorder] = React.useState(false);
    return (
        <Container>
            <Row>
                <Col>
                    <SuperEllipse
                        size='80'
                        backgroundColor='var(--color-light-bg-secondary)'
                        border={border && { color: 'var(--color-light-border-primary)' }}
                    >
                        <DiamondsMIcon />
                    </SuperEllipse>
                </Col>
            </Row>
            <Row>
                <Switch
                    block={true}
                    checked={border}
                    label='Обводка'
                    onChange={() => setBorder(!border)}
                />
            </Row>
        </Container>
    );
});
```

## Слоты

В зависимости от размера, компонент поддерживает один или несколько слотов.
Контент можно расположить в центральном слоте или в верхнем и нижнем аддоне.
В центральном слоте допустимо располагать иконки, в верхнем аддоне индикатор и другие
IconView cоответствующего размера, в нижнем аддоне только IconView cоответствующего размера.
Размер компонента так же определяет размер слотов и соответственно размеры контента который можно в них поместить.
Для шейпов `rectangle` и `noShape` верхний и нижний аддоны недоступны.

```jsx live
render(() => {
    const [content, setContent] = React.useState('icon');
    const [topAddons, setTopAddons] = React.useState('indicatorSmall');
    const [bottomAddons, setBottomAddons] = React.useState('iconView');

    const onChangeContent = (_, payload) => {
        setContent(payload.value);
    };

    const onChangeTopAddons = (_, payload) => {
        setTopAddons(payload.value);
    };

    const onChangeBottomAddons = (_, payload) => {
        setBottomAddons(payload.value);
    };

    const iconViewSvg = bottomAddons === 'iconView' && (
        <Circle size='24' backgroundIcon={ThumbUpCompactMIcon} />
    );
    const iconViewPositive = (
        <Circle size='24' backgroundColor='var(--color-light-graphic-positive)'>
            <CheckmarkCompactMIcon color='var(--color-static-graphic-light)' />
        </Circle>
    );
    const indicatorSmall = topAddons === 'indicatorSmall' && <Indicator view='red' />;
    const indicator = <Indicator value={1} view='red' height={24} />;

    const topAddonsView = {
        indicator,
        iconViewPositive,
    }[topAddons];

    return (
        <Container>
            <Row>
                <Col>
                    <SuperEllipse
                        size='80'
                        topAddons={topAddonsView}
                        indicator={indicatorSmall}
                        bottomAddons={iconViewSvg}
                    >
                        {content === 'icon' && <DiamondsMIcon />}
                    </SuperEllipse>
                </Col>
            </Row>

            <Row>
                <RadioGroup
                    label='Центральный слот'
                    name='radioGroup'
                    onChange={onChangeContent}
                    value={content}
                >
                    <Radio label='Иконка' value='icon' size='m' />
                    <Radio label='Без контента' value='noContent' size='m' />
                </RadioGroup>
            </Row>

            <Row>
                <RadioGroup
                    label='Верхний аддон'
                    name='radioGroup'
                    onChange={onChangeTopAddons}
                    value={topAddons}
                >
                    <Radio label='Маленький индикатор' value='indicatorSmall' size='m' />
                    <Radio label='Индикатор' value='indicator' size='m' />
                    <Radio label='IconView' value='iconViewPositive' size='m' />
                    <Radio label='Без контента' value='noContent' size='m' />
                </RadioGroup>
            </Row>

            <Row>
                <RadioGroup
                    label='Нижний аддон'
                    name='radioGroup'
                    onChange={onChangeBottomAddons}
                    value={bottomAddons}
                >
                    <Radio label='IconView' value='iconView' size='m' />
                    <Radio label='Без контента' value='noContent' size='m' />
                </RadioGroup>
            </Row>
        </Container>
    );
});
```

## Индикатор

Компонент [Indicator](?path=/docs/indicator--docs) может отображаться только в верхнем аддоне.
Если в индикатор не передан контент, рекомендуем отображать его компактную версию.

```jsx live
const styles = {
    display: 'flex',
    gap: '20px',
};

render(
    <div style={styles}>
        <SuperEllipse size='80' indicator={<Indicator view='red' />} />
        <SuperEllipse size='80' topAddons={<Indicator value={1} height={24} view='red' />} />
        <SuperEllipse size='80' topAddons={<Indicator value={25} height={24} view='red' />} />
        <SuperEllipse size='80' topAddons={<Indicator value={100} height={24} view='red' />} />
    </div>,
);
//MOBILE
const styles = {
    display: 'flex',
    gap: '20px',
};

render(
    <Container>
        <div style={styles}>
            <SuperEllipse size='80' indicator={<Indicator view='red' />} />
            <SuperEllipse size='80' topAddons={<Indicator value={1} height={24} view='red' />} />
            <SuperEllipse size='80' topAddons={<Indicator value={25} height={24} view='red' />} />
        </div>
        <Gap size='l' />
        <SuperEllipse size='80' topAddons={<Indicator value={100} height={24} view='red' />} />
    </Container>,
);
```
