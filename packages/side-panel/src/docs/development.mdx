import { ArgsTabs, CssVars } from 'storybook/blocks';
import { SidePanel, SidePanelDesktop, SidePanelMobile } from '..';
import vars from '!!raw-loader!../vars.css';

## Подключение

```jsx
// Атомарные импорты
import {
    SidePanelComponentDesktop,
    ContentDesktop,
    HeaderDesktop,
    FooterDesktop,
    ControlsDesktop,
} from '@alfalab/core-components/side-panel';
import {
    SidePanelComponentMobile,
    ContentMobile,
    HeaderMobile,
    FooterMobile,
    ControlsMobile,
} from '@alfalab/core-components/side-panel';
import {
    SidePanelComponentResponsive,
    ContentResponsive,
    HeaderResponsive,
    FooterResponsive,
    ControlsResponsive,
} from '@alfalab/core-components/side-panel';

// Компаунд импорты
import {
    SidePanelDesktop,
    SidePanelMobile,
    SidePanelResponsive,
} from '@alfalab/core-components/side-panel';
```

Из индекса также импортируется алиас responsive/compound версии компонента - SidePanel

## Использование dataTestId

В компоненте используются модификаторы для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getSidePanelTestIds`.
Импорт из `@alfalab/core-components/side-panel/shared`.

Функция возвращает объект:

```jsx
{
        modal: dataTestId,
        content: `${dataTestId}-content`,
        footer: `${dataTestId}-footer`,
        controls: `${dataTestId}-controls`,
        header: `${dataTestId}-header`,
        title: `${dataTestId}-header-title`,
        closer: `${dataTestId}-header-closer`,
        backButton: `${dataTestId}-header-back-button`,
};
```

## Свойства

<ArgsTabs
    components={{
        SidePanelDesktop,
        SidePanelMobile,
        SidePanelResponsive: SidePanel,
        'SidePanel.Header': SidePanel.Header,
        'SidePanel.Content': SidePanelDesktop.Content,
        'SidePanel.Footer': SidePanelDesktop.Footer,
        'SidePanel.Controls': SidePanel.Controls,
    }}
/>

## Переменные

<CssVars css={vars} />
