import { Meta, ArgsTable, Story } from '@storybook/addon-docs';
import { boolean, text } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { Backdrop } from '@alfalab/core-components-backdrop';

import { Backdrop as BackdropTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../vars.css';

<Meta
    title='Components/Backdrop'
    component={Backdrop}
    parameters={{ previewTabs: { canvas: { hidden: true } } }}
    id='Backdrop'
/>

<Story name='Backdrop'>
    <Backdrop
        open={boolean('open', false)}
        invisible={boolean('invisible', false)}
        unmountOnExit={boolean('unmountOnExit', false)}
    >
        {text('children', '')}
    </Backdrop>
</Story>

<!-- Docs -->

<ComponentHeader name='Backdrop' version={version} package='@alfalab/core-components/backdrop' />

```jsx
import { Backdrop } from '@alfalab/core-components/backdrop';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={BackdropTS} />}
    cssVars={<CssVars css={styles} />}
/>
