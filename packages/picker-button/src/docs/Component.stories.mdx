import { Meta, Story, Props } from '@storybook/addon-docs';
import { text, boolean, select } from '@storybook/addon-knobs';
import { CarMIcon } from '@alfalab/icons-glyph/CarMIcon';
import { StarMIcon } from '@alfalab/icons-glyph/StarMIcon';
import { CardTransferMIcon } from '@alfalab/icons-glyph/CardTransferMIcon';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { PickerButton } from '@alfalab/core-components-picker-button';
import { PickerButtonMobile } from '@alfalab/core-components-picker-button/mobile';
import { PickerButtonDesktop } from '@alfalab/core-components-picker-button/desktop';

import { PickerButton as PickerButtonTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';

export const options = [
    { key: 'Заказать карту' },
    { key: 'Выписка по счёту' },
    { key: 'Выпустить карту' },
    { key: 'Скопировать реквизиты' },
];

<Meta title='Компоненты/PickerButton' component={PickerButton} />

<!-- Canvas -->

<Story name='PickerButton'>
    <PickerButton
        options={options}
        label={text('label', 'Действие')}
        size={select('size', ['xxs', 'xs', 's', 'm', 'l', 'xl'], 'm')}
        view={select('view', ['link', 'filled', 'primary', 'secondary', 'outlined', 'ghost'], 'secondary')}
        variant={select('variant', ['default', 'compact'])}
        disabled={boolean('disabled', false)}
        loading={boolean('loading', false)}
        leftAddons={boolean('Иконка слева', false) ? <StarMIcon /> : null}
    />
</Story>

<Story name='PickerButtonMobile'>
    <PickerButtonMobile
        options={options}
        label={text('label', 'Действие')}
        size={select('size', ['xxs', 'xs', 's', 'm', 'l', 'xl'], 'm')}
        view={select('view', ['link', 'filled', 'primary', 'secondary', 'outlined', 'ghost'], 'secondary')}
        variant={select('variant', ['default', 'compact'])}
        disabled={boolean('disabled', false)}
        loading={boolean('loading', false)}
        leftAddons={boolean('Иконка слева', false) ? <StarMIcon /> : null}
    />
</Story>

<Story name='PickerButtonDesktop'>
    <PickerButtonDesktop
        options={options}
        label={text('label', 'Действие')}
        size={select('size', ['xxs', 'xs', 's', 'm', 'l', 'xl'], 'm')}
        view={select('view', ['link', 'filled', 'primary', 'secondary', 'outlined', 'ghost'], 'secondary')}
        variant={select('variant', ['default', 'compact'])}
        disabled={boolean('disabled', false)}
        loading={boolean('loading', false)}
        leftAddons={boolean('Иконка слева', false) ? <StarMIcon /> : null}
    />
</Story>

<!-- Docs -->

<ComponentHeader
    name='PickerButton'
    version={version}
    package='@alfalab/core-components/picker-button'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=3422%3A32180'
/>

```jsx
import { PickerButton } from '@alfalab/core-components/picker-button';
```

<Tabs
    description={<Description />}
    props={<Props of={PickerButtonTS} />}
    changelog={<Changelog />}
/>
