import { ArgsTable } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { ModalMobile as ModalMobileTS } from '../Component.mobile';
import { ModalDesktop as ModalDesktopTS } from '../Component.desktop';
import { ModalResponsive as ModalResponsiveTS } from '../Component.responsive';
import vars from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { ModalResponsive } from '@alfalab/core-components/modal/responsive';
import { ModalDesktop } from '@alfalab/core-components/modal/desktop';
import { ModalMobile } from '@alfalab/core-components/modal/mobile';
```

## Использование контекста

Компонент модального окна предоставляет доступ к контексту, что позволяет гибко настраивать и расширять компонент, а также упрощает использование.

```tsx
export type ModalContext = {
    // Указывает, используется ли футер
    hasFooter?: boolean;
    // Указывает, используется ли шапка (например, позволяет гибко настраивать отступы)
    hasHeader?: boolean;
    // Указывает наличие скролла
    hasScroll?: boolean;
    // Указывает, что контент прокручен
    headerHighlighted?: boolean;
    // Указывает, что контент прокручен не до конца
    footerHighlighted?: boolean;
    // Коллбэк, который должна вызвать кастомная шапка
    setHasHeader: (exists: boolean) => void;
    // Коллбэк, который должна вызвать кастомный футер
    setHasFooter: (exists: boolean) => void;
    // Позволяет закрыть модальное окно
    onClose: (event: React.MouseEvent<HTMLElement>) => void;
    // Реф, который должен быть установлен контентой области
    contentRef: React.Ref<HTMLElement>;
};
```

## Свойства

<ArgsTable
    components={{
        ModalDesktop: ModalDesktopTS,
        ModalMobile: ModalMobileTS,
        ModalResponsive: ModalResponsiveTS,
    }}
/>

## Переменные

<CssVars css={vars} />
