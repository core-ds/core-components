import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, boolean, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { CodeInput } from '@alfalab/core-components-code-input';

import { CodeInput as CodeInputTS } from '../component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../vars.css';

<Meta title='Components/CodeInput' component={CodeInput} id='CodeInput' />

<!-- Canvas -->

<Story name='CodeInput'>
    {React.createElement(() => {
        return (
            <CodeInput
                fields={number('fields', 4)}
                disabled={boolean('disabled', false)}
                error={text('error', '')}
                initialValues='1234'
            />
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader name='CodeInput' version={version} package='@alfalab/core-components/code-input' />

```jsx
import { CodeInput } from '@alfalab/core-components/code-input';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={CodeInputTS} />}
    cssVars={<CssVars css={styles} />}
/>
