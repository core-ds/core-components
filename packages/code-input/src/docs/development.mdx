import { ArgsTabs, CssVars } from 'storybook/blocks';
import { CodeInput} from '../index';
import { CodeInputDesktop} from '../desktop';
import { CodeInputMobile } from '../mobile';
import styles from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { CodeInput } from '@balafla/core-components/code-input';
import { CodeInputMobile } from '@balafla/core-components/code-input/mobile';
import { CodeInputDesktop } from '@balafla/core-components/code-input/desktop';
```
Из индекса импортируется responsive версия компонента.

## Формат сообщения

Более подробно об WebOTP Api можно почитать [тут](https://web.dev/i18n/ru/web-otp/).
Основные требования:
- Начинается с удобочитаемого текста, для того чтобы пользователь понял о чем речь;
- Содержит состоящий из 4–10 цифр код OTP;
- Последняя строка соответсвует формату: @доменная часть URL-адреса веб-сайта, вызвавшего API #OTP.

## Пример сообщения

```jsx
Your OTP is: 1234.
@example.com #1234
```

## Свойства

<ArgsTabs components={{ CodeInputDesktop, CodeInputMobile, CodeInput }} />

## Переменные

<CssVars css={styles} />
