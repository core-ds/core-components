HOC, позволяющий закрепить определенный текст после значения инпута.

Добавляет в переданный инпут доп. пропсу `suffix?: ReactNode`

```jsx live
const SuffixInput = withSuffix(Input);

render(<SuffixInput suffix={<b> мес</b>} />);
```
