import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select, boolean } from '@storybook/addon-knobs';
import { formatDate } from '@alfalab/core-components-date-input';
import { CalendarInput } from '@alfalab/core-components-calendar-input';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { CalendarInputResponsive as CalendarInputTS } from '../responsive';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../components/calendar-input/index.module.css';

<Meta title='Components/Forms/Inputs/CalendarInput' component={CalendarInput} />

<!-- Canvas -->

<Story name='CalendarInput'>
    {React.createElement(() => {
        const [value, setValue] = React.useState(formatDate(new Date()));
        const handleChange = (event, { value }) => setValue(value);
        return (
            <CalendarInput
                value={value}
                onChange={handleChange}
                label={text('label', 'Дата регистрации')}
                placeholder={text('placeholder', '')}
                mobileMode={select('mobileMode', ['native', 'input', 'popover'], 'popover')}
                preventFlip={boolean('preventFlip', false)}
                block={boolean('block', false)}
                success={boolean('success', false)}
                error={boolean('error', false)}
                disabled={boolean('disabled', false)}
                readOnly={boolean('readOnly', false)}
            />
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='CalendarInput'
    version={version}
    package='@alfalab/core-components/calendar-input'
/>

```jsx
import { CalendarInput } from '@alfalab/core-components/calendar-input';
import { CalendarInputDesktop } from '@alfalab/core-components/calendar-input/desktop';
import { CalendarInputMobile } from '@alfalab/core-components/calendar-input/mobile';
import { CalendarInputResponsive } from '@alfalab/core-components/calendar-input/responsive';
```

Из индекса импортируется responsive версия компонента.

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={CalendarInputTS} />}
    cssVars={<CssVars css={styles} />}
/>
