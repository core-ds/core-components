import { Meta, Story, Markdown } from '@storybook/addon-docs';
import { boolean, select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';
import { NextSelect } from '@alfalab/core-components-next-select';

import Description from './description.mdx';
import Development from './development.mdx';
import Changelog from '../../CHANGELOG.md?raw';

<Meta title='Components/NextSelect' component={NextSelect} id='NextSelect' />

{/* Canvas */}

{/* Docs */}

<ComponentHeader
    name='NextSelect'
    children='Используется для индикации выполнения команды пользователя.'
/>

<Tabs
    description={<Description />}
    development={<Development />}
    changelog={<Markdown>{Changelog}</Markdown>}
/>
