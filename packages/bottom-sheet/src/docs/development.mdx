import { ArgTypes } from '@storybook/addon-docs';
import { CssVars } from 'storybook/blocks';
import { BottomSheet } from '../index';
import styles from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { BottomSheet } from '@balafla/core-components/bottom-sheet';
```

## Использование dataTestId

В компоненте используются модификаторы для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getBottomSheetTestIds`.
Импорт из `@balafla/core-components/bottom-sheet/shared`.

Функция возвращает объект:

```jsx
{
    bottomSheet: dataTestId,
    content: `${dataTestId}-content`,
    footer: `${dataTestId}-footer`,
    header: `${dataTestId}-header`,
    title: `${dataTestId}-header-title`,
    closer: `${dataTestId}-header-closer`,
    backButton: `${dataTestId}-header-back-button`,
};
```

## Свойства

<ArgTypes of={BottomSheet} />

## Переменные

<CssVars css={styles} />
