import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs';

import Description from './description.mdx';

import { ButtonTest } from '../';

<Meta
    title="Компоненты/ButtonTest"
    component={ButtonTest}
    argTypes={{
        view: {
            options: ['primary', 'secondary', 'tertiary', 'link', 'ghost'],
            control: { type: 'select' },
            description: 'kkkkk',
        },
        leftAddons: {
            options: '<StarMIcon />',
            control: { type: 'boolean' },
        },
        rightAddons: {
            options: '<StarMIcon />',
            control: { type: 'boolean' },
        },
        Component: {
            control: false,
        },
        dataTestId: {
            control: false,
        },
        className: {
            control: false,
        },
    }}
/>

export const Template = (args) => <ButtonTest {...args}>{args.children}</ButtonTest>;

<Story name="Primary" defaultCanvas>
    {Template.bind({})}
</Story>

<ArgsTable story="Primary" />

<Description />