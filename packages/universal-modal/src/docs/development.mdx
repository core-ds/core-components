import { ArgsTabs, CssVars } from 'storybook/blocks';
import { SidePanelMobile } from '../mobile';
import { UniversalModalDesktop } from '../desktop';
import { UniversalModalResponsive } from '..';
import vars from '!!raw-loader!../vars.css';

## Подключение

```jsx
import { UniversalModalResponsive } from '@alfalab/core-components/universal-modal';
import { UniversalModalDesktop } from '@alfalab/core-components/universal-modal/desktop';
import { UniversalModalMobile } from '@alfalab/core-components/universal-modal/mobile';
```

Из индекса импортируется responsive версия компонента.

## Использование dataTestId

В компоненте используются модификаторы для `dataTestId`.
Для удобного поиска элементов можно воспользоваться функцией `getSidePanelTestIds`.
Импорт из `@alfalab/core-components/side-panel/shared`.

Функция возвращает объект:

```jsx
{
        modal: dataTestId,
        content: `${dataTestId}-content`,
        footer: `${dataTestId}-footer`,
        controls: `${dataTestId}-controls`,
        header: `${dataTestId}-header`,
        title: `${dataTestId}-header-title`,
        closer: `${dataTestId}-header-closer`,
        backButton: `${dataTestId}-header-back-button`,
};
```

## Свойства

<ArgsTabs
    components={{
        UniversalModalDesktop,
        SidePanelMobile,
        UniversalModalResponsive,
        'SidePanel.Header': UniversalModalResponsive.Header,
        'SidePanel.Content': UniversalModalDesktop.Content,
        'SidePanel.Footer': UniversalModalDesktop.Footer,
        'SidePanel.Controls': UniversalModalResponsive.Controls,
    }}
/>

## Переменные

<CssVars css={vars} />
