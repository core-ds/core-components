import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { ModeChecker } from 'storybook/components/mode-checker';
import { Comment } from '@alfalab/core-components-comment';

import { Comment as CommentTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';
import {
    stylesStringToObj,
    getQueryParam,
} from '../../../screenshot-utils/screenshots-story/utils';

<Meta title='Components/Comment' component={Comment} id='Comment' />

<!-- Canvas -->

<Story name='Comment'>
    {React.createElement(() => {
        const children = text('Текст комментария', 'Comment');
        const rowLimit = select('Количество строк', ['', '2', '5'], '');
        const previewStyles = stylesStringToObj(getQueryParam('wrapperStyles'));
        return (
            <div style={previewStyles}>
                <style>{`.comment{background: var(--color-light-specialbg-secondary-transparent)}`}</style>
                <ModeChecker />
                <Comment rowLimit={rowLimit} className='comment'>
                    {children}
                </Comment>
            </div>
        );
    })}
</Story>

<!-- Docs -->

<ComponentHeader
    name='Comment'
    version={version}
    package='@alfalab/core-components/comment-view'
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=62541%3A72707'
/>

```jsx
import { Comment } from '@alfalab/core-components/comment';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={CommentTS} />}
    cssVars={<CssVars css={styles} />}
/>
