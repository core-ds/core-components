## Примеры

```jsx live
<CardImage
    cardId='EG'
    width={isMobile() ? 320 : 380}
    baseUrl={`https://${AO_ENV}/cards-images/cards/`}
    className='some name'
    id='some id'
    dataTestId='test id'
/>
```
