## Примеры

```jsx live
<CardImage
    cardId='EG'
    width={isMobile() ? 320 : 380}
    baseUrl={process.env.CORE_COMPONENTS_CARD_IMAGE_BASE_URL}
    className='some name'
    id='some id'
    dataTestId='test id'
/>
```
