import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { text, boolean, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { CardImage } from '@alfalab/core-components-card-image';

import { CardImage as CardImageTS } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

<Meta title='Компоненты/Другое/CardImage' component={CardImage} />

<!-- Canvas -->

<Story name='CardImage'>
    <CardImage
        cardId={text('cardId', 'EG')}
        layers={text(
            'layers',
            'BACKGROUND,CARD_NUMBER,CARD_HOLDER,PAY_PASS,CHIP,LOGO,PAYMENT_SYSTEM,RESERVED_1,RESERVED_2,VALID_DATE',
        )}
        width={number('width', 280)}
        rounded={boolean('rounded', false)}
        baseUrl={text('baseUrl', 'https://online.alfabank.ru/cards-images/cards/')}
    />
</Story>

<!-- Docs -->

<ComponentHeader name='CardImage' version={version} package='@alfalab/core-components/card-image' />

```jsx
import { CardImage } from '@alfalab/core-components/card-image';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<ArgsTable of={CardImageTS} />}
    cssVars={<CssVars css={styles} />}
/>
