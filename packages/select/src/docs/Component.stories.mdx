import { Meta, Story, Props } from '@storybook/addon-docs';
import { text, boolean, number, select as selectKnob } from '@storybook/addon-knobs';

import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';
import { Select } from '@alfalab/core-components-select';
import { SelectMobile } from '@alfalab/core-components-select/components/select-mobile';
import { SelectModalMobile } from '@alfalab/core-components-select/components/select-modal-mobile';
import { Arrow } from '@alfalab/core-components-select/components/arrow';

import { Select as SelectTS } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import vars from '!!raw-loader!../vars.css';

export const options = [
    { key: '1', content: 'Neptunium' },
    { key: '2', content: 'Plutonium' },
    { key: '3', content: 'Americium' },
    { key: '4', content: 'Curium' },
    { key: '5', content: 'Berkelium' },
    { key: '6', content: 'Californium' },
    { key: '7', content: 'Einsteinium' },
    { key: '8', content: 'Fermium' },
    { key: '9', content: 'Vanadium' },
    { key: '10', content: 'Manganese' },
    { key: '11', content: 'Silicon' },
    { key: '12', content: 'Titanium' },
    { key: '13', content: 'Neptunium' },
    { key: '14', content: 'Plutonium' },
    { key: '15', content: 'Americium' },
    { key: '16', content: 'Curium' },
    { key: '17', content: 'Berkelium' },
];

export const POSITION_OPTIONS = [
    'top',
    'top-start',
    'top-end',
    'bottom',
    'bottom-start',
    'bottom-end',
    'right',
    'right-start',
    'right-end',
    'left',
    'left-start',
    'left-end',
];

<Meta
    title='Компоненты/Select'
    component={Select}
/>

<!-- Canvas -->

<Story name='Select'>
    <Select
        block={boolean('block', false)}
        size={selectKnob('size', ['s', 'm', 'l', 'xl'], 's')}
        disabled={boolean('disabled', false)}
        error={text('error', '')}
        hint={text('hint', '')}
        options={options}
        multiple={boolean('multiple', false)}
        allowUnselect={boolean('allowUnselect', true)}
        closeOnSelect={boolean('closeOnSelect', true)}
        Arrow={boolean('Arrow', true) ? Arrow : () => null}
        circularNavigation={boolean('circularNavigation', false)}
        nativeSelect={boolean('nativeSelect', false)}
        placeholder={text('placeholder', 'Выберите элемент')}
        label={text('label', 'Элемент')}
        labelView={selectKnob('labelView', ['inner', 'outer'], 'inner')}
        visibleOptions={number('visibleOptions', 5)}
        defaultOpen={boolean('defaultOpen', false)}
        popoverPosition={selectKnob('popoverPosition', POSITION_OPTIONS, 'bottom-start')}
        optionsListWidth={selectKnob('optionsListWidth', ['field', 'content'], 'field')}
    />
</Story>

<Story name='SelectMobile'>
    <SelectMobile
        block={boolean('block', false)}
        size={selectKnob('size', ['s', 'm', 'l', 'xl'], 's')}
        disabled={boolean('disabled', false)}
        error={text('error', '')}
        hint={text('hint', '')}
        options={options}
        multiple={boolean('multiple', false)}
        allowUnselect={boolean('allowUnselect', true)}
        closeOnSelect={boolean('closeOnSelect', true)}
        Arrow={boolean('Arrow', true) ? Arrow : () => null}
        circularNavigation={boolean('circularNavigation', false)}
        placeholder={text('placeholder', 'Выберите элемент')}
        label={text('label', 'Элемент')}
        labelView={selectKnob('labelView', ['inner', 'outer'], 'inner')}
        defaultOpen={boolean('defaultOpen', false)}
        swipeable={boolean('swipeable', true)}
    />
</Story>

<Story name='SelectModalMobile'>
    <SelectModalMobile
        block={boolean('block', false)}
        size={selectKnob('size', ['s', 'm', 'l', 'xl'], 's')}
        disabled={boolean('disabled', false)}
        error={text('error', '')}
        hint={text('hint', '')}
        options={options}
        multiple={boolean('multiple', true)}
        allowUnselect={boolean('allowUnselect', true)}
        closeOnSelect={boolean('closeOnSelect', true)}
        Arrow={boolean('Arrow', true) ? Arrow : () => null}
        circularNavigation={boolean('circularNavigation', false)}
        placeholder={text('placeholder', 'Выберите элемент')}
        label={text('label', 'Элемент')}
        labelView={selectKnob('labelView', ['inner', 'outer'], 'inner')}
        defaultOpen={boolean('defaultOpen', false)}
    />
</Story>

<!-- Docs -->

<ComponentHeader
    name='Select'
    version={version}
    package='@alfalab/core-components/select'
    stage={2}
    design='https://www.figma.com/file/cdNnkh2QdxuvYLrBm4cubM/Web-%3A%3A-Core-Default-Components?node-id=184%3A19405'
/>

```jsx
import { Select } from '@alfalab/core-components/select';
```

<Tabs
    description={<Description />}
    props={<Props of={SelectTS} />}
    cssVars={<CssVars css={vars} />}
    changelog={<Changelog />}
/>
