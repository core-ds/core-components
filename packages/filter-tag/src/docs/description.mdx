## Группа фильтров

Обычно фильтры объединяются в группу, в рамках группы рекомендуется использовать один паттерн отображения лейбла и контролов. У выбранного тега может отображаться шеврон, крестик или и то и другое. Лейбл в выбранном состоянии можно скрывать для экономии места. Но если выбрано несколько значений, лейбл нужно оставить.

```jsx live
// Одиночный выбор
const CustomField = ({
    hideFieldLabel,
    selected,
    setSelectedItems,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => {
    const checkedContent = (
        <span>
            {!hideFieldLabel && 'Одиночный выбор:'} {selected && selected.content}
        </span>
    );

    const contentLabel = <span>Одиночный выбор</span>;

    return (
        <div ref={ref}>
            <FilterTagDesktop
                onClear={() => setSelectedItems([])}
                view='filled'
                checked={selected}
                {...restInnerProps}
                {...restProps}
            >
                {selected ? checkedContent : contentLabel}
            </FilterTagDesktop>
        </div>
    );
};

// Множественный выбор
const CustomFieldMultiple = ({
    hideFieldLabel,
    selected,
    setSelectedItems,
    selectedMultiple,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => {
    const content = selected && selected.content;

    const checkedContent =
        selectedMultiple.length > 1 ? (
            <span>Множественный выбор ({selectedMultiple.length})</span>
        ) : (
            <span>
                {!hideFieldLabel && 'Множественный выбор:'} {content}
            </span>
        );

    const contentLabel = <span>Множественный выбор</span>;

    return (
        <div ref={ref}>
            <FilterTagDesktop
                onClear={() => setSelectedItems([])}
                view='filled'
                checked={selected}
                {...restInnerProps}
                {...restProps}
            >
                {selected ? checkedContent : contentLabel}
            </FilterTagDesktop>
        </div>
    );
};

render(() => {
    const FIELD_SIZE = 40;
    const FIELD_SHAPE = 'rectangular';

    const controlMap = {
        chevron: { showArrow: true, showClear: false },
        clear: { showArrow: false, showClear: true },
        both: { showArrow: true, showClear: true },
    };

    const options = React.useMemo(
        () => [
            { key: '1', content: 'Aurum' },
            { key: '2', content: 'Bercelium' },
            { key: '3', content: 'Curium' },
            { key: '4', content: 'Neptunium' },
            { key: '5', content: 'Plutonuim' },
        ],
        [],
    );

    const [controls, setControls] = React.useState('chevron');
    const [hideLabel, setHideLabel] = React.useState(false);
    const { showArrow, showClear } = controlMap[controls];

    const fieldProps = {
        size: FIELD_SIZE,
        shape: FIELD_SHAPE,
        showArrow,
        showClear,
        hideFieldLabel: hideLabel,
    };

    const [selected, setSelected] = React.useState([]);

    const handleChangeSelected = ({ selectedMultiple }) => {
        setSelected(selectedMultiple);
    };

    const [selectedMultiple, setSelectedMultiple] = React.useState([]);
    const handleChangeMultiple = ({ selectedMultiple }) => {
        setSelectedMultiple(selectedMultiple.map((option) => option.key));
    };

    // Диапазон значений
    const [open, setOpen] = React.useState(false);
    const { selectedFrom, selectedTo, updatePeriod, resetPeriod } = usePeriod();
    const calendarRef = React.useRef(null);
    const anchorRef = React.useRef(null);

    const handleOpen = () => {
        setOpen(true);
    };

    const handleClose = () => {
        setOpen(false);
    };

    const handleClear = () => {
        handleClose();
        resetPeriod();
    };

    const handleUpdatePeriod = (date) => {
        updatePeriod(date);

        if (selectedFrom) {
            handleClose();
        }
    };

    const handleWrapperBlur = (event) => {
        const target = event.relatedTarget || document.activeElement;

        if (
            (anchorRef.current && anchorRef.current.contains(target)) ||
            (calendarRef.current && calendarRef.current.contains(target))
        ) {
            return;
        }

        handleClose();
    };

    const getDateString = React.useCallback((date) => {
        const day = date.getDate() < 10 ? `0${date.getDate()}` : date.getDate();
        const month = date.getMonth() + 1 < 10 ? `0${date.getMonth() + 1}` : date.getMonth() + 1;
        const year = date.getFullYear();

        return `${day}.${month}.${year}`;
    }, []);

    const selectedRange = React.useMemo(() => {
        if (selectedFrom && selectedTo) {
            const selectedFromDate = new Date(selectedFrom);
            const selectedToDate = new Date(selectedTo);
            return `${getDateString(selectedFromDate)} – ${getDateString(selectedToDate)}`;
        }
        return '';
    }, [selectedFrom, selectedTo]);

    const checkedContent = (
        <span>
            {!hideLabel && 'Диапазон значений:'} {selectedRange && selectedRange}
        </span>
    );
    const content = <span>Диапазон значений</span>;

    return (
        <>
            <style>
                {`
                    .filter-tag-options-list-desktop {
                        width: 200px;
                        min-width: 200px !important;
                    }
                    .filter-tag-calendar-popover {
                        border-radius: 0;
                        border: none;
                        background: transparent;
                        box-shadow: none;
                    }
                    .filter-tag-calendar-panel {
                        border-radius: var(--OptionListCornerRadius, 16px);
                        border: 1px solid var(--neutral-300, #E7E8EB);
                        background: var(--modal-bg-primary, #FFF);
                        box-shadow:
                            0 20px 24px 0 rgba(0, 0, 0, 0.08),
                            0 12px 16px 0 rgba(0, 0, 0, 0.04),
                            0 4px 8px 0 rgba(0, 0, 0, 0.04),
                            0 0 1px 0 rgba(0, 0, 0, 0.04);
                        overflow: hidden;
                    }
                `}
            </style>
            <SelectDesktop
                allowUnselect={true}
                popoverPosition='bottom-start'
                options={options}
                Field={CustomField}
                onChange={handleChangeSelected}
                selected={selected}
                Option={BaseOption}
                fieldProps={fieldProps}
                optionsListClassName='filter-tag-options-list-desktop'
            />
            <Gap size={16} />
            <SelectDesktop
                allowUnselect={true}
                popoverPosition='bottom-start'
                options={options}
                Field={CustomFieldMultiple}
                onChange={handleChangeMultiple}
                selected={selectedMultiple}
                multiple={true}
                fieldProps={fieldProps}
                optionsListClassName='filter-tag-options-list-desktop'
            />
            <Gap size={16} />
            <div ref={anchorRef} tabIndex={-1} onBlur={handleWrapperBlur}>
                <FilterTagDesktop
                    size={FIELD_SIZE}
                    shape={FIELD_SHAPE}
                    view='filled'
                    showArrow={showArrow}
                    showClear={showClear}
                    onClick={handleOpen}
                    onClear={handleClear}
                    checked={Boolean(selectedRange)}
                    open={open}
                >
                    {selectedRange ? checkedContent : content}
                </FilterTagDesktop>
                <Popover
                    anchorElement={anchorRef.current}
                    offset={[0, 4]}
                    open={open}
                    preventFlip={true}
                    position='bottom-start'
                    withTransition={false}
                    popperClassName='filter-tag-calendar-popover'
                >
                    <div
                        className='filter-tag-calendar-panel'
                        onMouseDown={(event) => event.preventDefault()}
                    >
                        <CalendarDesktop
                            ref={calendarRef}
                            responsive={true}
                            selectedFrom={selectedFrom}
                            selectedTo={selectedTo}
                            onChange={handleUpdatePeriod}
                            selectorView='month-only'
                        />
                    </div>
                </Popover>
            </div>

            <Gap size={24} />

            <RadioGroup
                direction='vertical'
                label='Контролы'
                name='filter-controls'
                onChange={(_, payload) => setControls(payload.value)}
                value={controls}
            >
                <Radio label='Шеврон' value='chevron' />
                <Radio label='Крестик' value='clear' />
                <Radio label='Шеврон и крестик' value='both' />
            </RadioGroup>

            <Gap size={16} />

            <RadioGroup
                direction='vertical'
                label='Лейбл в выбранном состоянии'
                name='filter-label'
                onChange={(_, payload) => setHideLabel(payload.value === 'hide')}
                value={hideLabel ? 'hide' : 'show'}
            >
                <Radio label='Показывать' value='show' />
                <Radio label='Скрывать' value='hide' />
            </RadioGroup>
        </>
    );
});
//MOBILE
// Одиночный выбор
const CustomField = ({
    hideFieldLabel,
    selected,
    setSelectedItems,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => {
    const checkedContent = (
        <span>
            {!hideFieldLabel && 'Одиночный выбор:'} {selected && selected.content}
        </span>
    );

    const contentLabel = <span>Одиночный выбор</span>;

    return (
        <div ref={ref}>
            <FilterTagMobile
                onClear={() => setSelectedItems([])}
                view='filled'
                checked={selected}
                {...restInnerProps}
                {...restProps}
            >
                {selected ? checkedContent : contentLabel}
            </FilterTagMobile>
        </div>
    );
};

// Множественный выбор
const CustomFieldMultiple = ({
    hideFieldLabel,
    selected,
    setSelectedItems,
    selectedMultiple,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => {
    const content = selected && selected.content;

    const checkedContent =
        selectedMultiple.length > 1 ? (
            <span>Множественный выбор ({selectedMultiple.length})</span>
        ) : (
            <span>
                {!hideFieldLabel && 'Множественный выбор:'} {content}
            </span>
        );

    const contentLabel = <span>Множественный выбор</span>;

    return (
        <div ref={ref}>
            <FilterTagMobile
                onClear={() => setSelectedItems([])}
                view='filled'
                checked={selected}
                {...restInnerProps}
                {...restProps}
            >
                {selected ? checkedContent : contentLabel}
            </FilterTagMobile>
        </div>
    );
};

render(() => {
    const FIELD_SIZE = 40;
    const FIELD_SHAPE = 'rectangular';

    const controlMap = {
        chevron: { showArrow: true, showClear: false },
        clear: { showArrow: false, showClear: true },
        both: { showArrow: true, showClear: true },
    };

    const options = [
        { key: '1', content: 'Aurum' },
        { key: '2', content: 'Bercelium' },
        { key: '3', content: 'Curium' },
        { key: '4', content: 'Neptunium' },
        { key: '5', content: 'Plutonuim' },
    ];

    const [controls, setControls] = React.useState('chevron');
    const [hideLabel, setHideLabel] = React.useState(false);
    const { showArrow, showClear } = controlMap[controls];

    const fieldProps = {
        size: FIELD_SIZE,
        shape: FIELD_SHAPE,
        showArrow,
        showClear,
        hideFieldLabel: hideLabel,
    };

    const [selected, setSelected] = React.useState([]);

    const handleChangeSelected = ({ selectedMultiple }) => {
        setSelected(selectedMultiple);
    };

    const [selectedMultiple, setSelectedMultiple] = React.useState([]);
    const handleChangeMultiple = ({ selectedMultiple }) => {
        setSelectedMultiple(selectedMultiple.map((option) => option.key));
    };

    // Диапазон значений
    const [open, setOpen] = React.useState(false);
    const { selectedFrom, selectedTo, updatePeriod, resetPeriod } = usePeriod();

    const handleOpen = () => {
        setOpen(true);
    };

    const handleClear = () => {
        setOpen(false);
        resetPeriod();
    };

    const handleUpdatePeriod = (date) => {
        updatePeriod(date);

        if (selectedFrom) {
            setOpen(false);
        }
    };

    const getDateString = React.useCallback((date) => {
        const day = date.getDate() < 10 ? `0${date.getDate()}` : date.getDate();
        const month = date.getMonth() + 1 < 10 ? `0${date.getMonth() + 1}` : date.getMonth() + 1;
        const year = date.getFullYear();

        return `${day}.${month}.${year}`;
    }, []);

    const selectedRange = React.useMemo(() => {
        if (selectedFrom && selectedTo) {
            const selectedFromDate = new Date(selectedFrom);
            const selectedToDate = new Date(selectedTo);
            return `${getDateString(selectedFromDate)} - ${getDateString(selectedToDate)}`;
        }
        return '';
    }, [selectedFrom, selectedTo]);

    const checkedContent = (
        <span>
            {!hideLabel && 'Диапазон значений:'} {selectedRange && selectedRange}
        </span>
    );
    const content = <span>Диапазон значений</span>;

    return (
        <>
            <style>
                {`
                    .filter-tag-calendar-mobile [class*='dayNames'],
                    [data-test-id='filter-tag-calendar-mobile'] [class*='dayNames'] {
                        padding-left: var(--gap-16);
                        padding-right: var(--gap-16);
                    }
                    .filter-tag-calendar-mobile [class*='virtuoso'],
                    [data-test-id='filter-tag-calendar-mobile'] [class*='virtuoso'] {
                        margin-left: var(--gap-16);
                        margin-right: var(--gap-16);
                    }
                `}
            </style>
            <SelectMobile
                placeholder='Одиночный выбор'
                allowUnselect={true}
                popoverPosition='bottom-start'
                options={options}
                Field={CustomField}
                onChange={handleChangeSelected}
                selected={selected}
                fieldProps={fieldProps}
                Option={BaseOption}
                optionProps={{ checkmarkPosition: 'after' }}
            />
            <Gap size={16} />
            <SelectMobile
                placeholder='Множественный выбор'
                allowUnselect={true}
                popoverPosition='bottom-start'
                options={options}
                Field={CustomFieldMultiple}
                onChange={handleChangeMultiple}
                selected={selectedMultiple}
                multiple={true}
                fieldProps={fieldProps}
                Option={BaseOption}
                optionProps={{ checkmarkPosition: 'after' }}
            />
            <Gap size={16} />
            <CalendarMobile
                className='filter-tag-calendar-mobile'
                dataTestId='filter-tag-calendar-mobile'
                open={open}
                onClose={() => setOpen(false)}
                selectedFrom={selectedFrom}
                selectedTo={selectedTo}
                onChange={handleUpdatePeriod}
                selectorView='month-only'
            />
            <FilterTagMobile
                size={FIELD_SIZE}
                shape={FIELD_SHAPE}
                view='filled'
                showArrow={showArrow}
                showClear={showClear}
                onClick={handleOpen}
                onClear={handleClear}
                checked={Boolean(selectedRange)}
                open={open}
            >
                {selectedRange ? checkedContent : content}
            </FilterTagMobile>

            <Gap size={24} />

            <RadioGroup
                direction='vertical'
                label='Контролы'
                name='filter-controls-mobile'
                onChange={(_, payload) => setControls(payload.value)}
                value={controls}
            >
                <Radio label='Шеврон' value='chevron' />
                <Radio label='Крестик' value='clear' />
                <Radio label='Шеврон и крестик' value='both' />
            </RadioGroup>

            <Gap size={16} />

            <RadioGroup
                direction='vertical'
                label='Лейбл в выбранном состоянии'
                name='filter-label-mobile'
                onChange={(_, payload) => setHideLabel(payload.value === 'hide')}
                value={hideLabel ? 'hide' : 'show'}
            >
                <Radio label='Показывать' value='show' />
                <Radio label='Скрывать' value='hide' />
            </RadioGroup>
        </>
    );
});
```

## Предзаполненное значение

Компонент может иметь предзаполненное значение, в таком случае, в заполненном состоянии у него не может быть крестика очистки.

```jsx live
const CustomField = ({
    label,
    selected,
    setSelectedItems,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => (
    <div ref={ref}>
        <FilterTagDesktop checked={selected} showClear={false} {...restInnerProps} {...restProps}>
            {selected.content}
        </FilterTagDesktop>
    </div>
);
render(() => {
    const options = React.useMemo(
        () => [
            { key: '1', content: 'По возрастанию' },
            { key: '2', content: 'По убыванию' },
        ],
        [],
    );

    const [selected, setSelected] = React.useState([options[0]]);

    const handleChangeSelected = ({ selectedMultiple }) => {
        setSelected(selectedMultiple);
    };

    return (
        <>
            <style>
                {`
                    .filter-tag-options-list-desktop {
                        width: 200px;
                        min-width: 200px !important;
                    }
                `}
            </style>
            <SelectDesktop
                popoverPosition='bottom-start'
                options={options}
                Field={CustomField}
                onChange={handleChangeSelected}
                selected={selected}
                fieldProps={{ size: 40, shape: 'rectangular' }}
                Option={BaseOption}
                optionsListClassName='filter-tag-options-list-desktop'
            />
        </>
    );
});
//MOBILE
const CustomField = ({
    label,
    selected,
    setSelectedItems,
    innerProps: { ref, ...restInnerProps },
    ...restProps
}) => (
    <div ref={ref}>
        <FilterTagMobile checked={selected} showClear={false} {...restInnerProps} {...restProps}>
            {selected.content}
        </FilterTagMobile>
    </div>
);
render(() => {
    const options = React.useMemo(
        () => [
            { key: '1', content: 'По возрастанию' },
            { key: '2', content: 'По убыванию' },
        ],
        [],
    );

    const [selected, setSelected] = React.useState([options[0]]);

    const handleChangeSelected = ({ selectedMultiple }) => {
        setSelected(selectedMultiple);
    };

    return (
        <>
            <SelectMobile
                popoverPosition='bottom-start'
                options={options}
                Field={CustomField}
                onChange={handleChangeSelected}
                selected={selected}
                fieldProps={{ size: 40, shape: 'rectangular' }}
                Option={BaseOption}
                optionProps={{ checkmarkPosition: 'after' }}
            />
        </>
    );
});
```

## Состояния и стили

У тега есть две опции, отвечающие за внешний вид. Взаимодействие с ним можно ограничить с помощью свойства `disabled`. Блокировать теги с выбранным значением не рекомендуется.

```jsx live
render(() => {
    const [disabled, setDisabled] = React.useState(false);
    const [shape, setShape] = React.useState('rectangular');
    const [view, setView] = React.useState('filled');

    const tagProps = {
        size: 40,
        shape,
        view,
        disabled,
    };

    const checkedContent = <span>Label: Value</span>;

    const tagBackdropStyle = {
        display: 'inline-flex',
        padding: 20,
        alignItems: 'flex-start',
        gap: 8,
        borderRadius: 20,
        border: '1px solid var(--color-light-neutral-300)',
        background:
            view === 'muted'
                ? 'var(--color-light-base-bg-secondary)'
                : 'var(--color-light-base-bg-primary)',
    };

    return (
        <Container>
            <div style={tagBackdropStyle}>
                <FilterTagDesktop {...tagProps} checked={false} showClear={false}>
                    <span>Label</span>
                </FilterTagDesktop>

                <FilterTagDesktop {...tagProps} checked={true} showClear={false}>
                    {checkedContent}
                </FilterTagDesktop>

                <FilterTagDesktop {...tagProps} checked={true} showArrow={false}>
                    {checkedContent}
                </FilterTagDesktop>

                <FilterTagDesktop {...tagProps} checked={true}>
                    {checkedContent}
                </FilterTagDesktop>
            </div>

            <Gap size={24} />

            <div style={{ width: '100%', alignSelf: 'stretch' }}>
                <Divider />
            </div>

            <Gap size={24} />

            <RadioGroup
                direction='vertical'
                label='Shape'
                name='filter-tag-states-shape'
                onChange={(_, p) => setShape(p.value)}
                value={shape}
            >
                <Radio label='Rectangular' value='rectangular' />
                <Radio label='Rounded' value='rounded' />
            </RadioGroup>

            <Gap size={16} />

            <RadioGroup
                direction='vertical'
                label='View'
                name='filter-tag-states-view'
                onChange={(_, p) => setView(p.value)}
                value={view}
            >
                <Radio label='Filled' value='filled' />
                <Radio label='Muted' value='muted' />
            </RadioGroup>

            <Gap size={32} />

            <div style={{ width: '100%', alignSelf: 'stretch' }}>
                <Divider />
            </div>

            <Gap size={20} />

            <Switch
                label='Заблокированное состояние'
                checked={disabled}
                onChange={() => setDisabled((prevState) => !prevState)}
            />
        </Container>
    );
});
//MOBILE
render(() => {
    const [disabled, setDisabled] = React.useState(false);
    const [shape, setShape] = React.useState('rectangular');
    const [view, setView] = React.useState('filled');

    const tagProps = {
        size: 40,
        shape,
        view,
        disabled,
    };

    const checkedContent = <span>Label: Value</span>;

    const tagBackdropStyle = {
        display: 'inline-flex',
        flexDirection: 'column',
        padding: 20,
        alignItems: 'flex-start',
        gap: 24,
        borderRadius: 20,
        border: '1px solid var(--color-light-neutral-300)',
        background:
            view === 'muted'
                ? 'var(--color-light-base-bg-secondary)'
                : 'var(--color-light-base-bg-primary)',
    };

    return (
        <Container>
            <div style={tagBackdropStyle}>
                <FilterTagMobile {...tagProps} checked={false} showClear={false}>
                    <span>Label</span>
                </FilterTagMobile>

                <FilterTagMobile {...tagProps} checked={true} showClear={false}>
                    {checkedContent}
                </FilterTagMobile>

                <FilterTagMobile {...tagProps} checked={true} showArrow={false}>
                    {checkedContent}
                </FilterTagMobile>

                <FilterTagMobile {...tagProps} checked={true}>
                    {checkedContent}
                </FilterTagMobile>
            </div>

            <Gap size={20} />

            <RadioGroup
                direction='vertical'
                label='Shape'
                name='filter-tag-states-shape-mobile'
                onChange={(_, p) => setShape(p.value)}
                value={shape}
            >
                <Radio label='Rectangular' value='rectangular' />
                <Radio label='Rounded' value='rounded' />
            </RadioGroup>

            <Gap size={16} />

            <RadioGroup
                direction='vertical'
                label='View'
                name='filter-tag-states-view-mobile'
                onChange={(_, p) => setView(p.value)}
                value={view}
            >
                <Radio label='Filled' value='filled' />
                <Radio label='Muted' value='muted' />
            </RadioGroup>

            <Gap size={32} />

            <div style={{ width: '100%', alignSelf: 'stretch' }}>
                <Divider />
            </div>

            <Gap size={20} />

            <Switch
                label='Заблокированное состояние'
                checked={disabled}
                onChange={() => setDisabled((prevState) => !prevState)}
            />
        </Container>
    );
});
```

## Дополнительный слот

С помощью слота leftAddons можно кастомизировать фильтртэг.

```jsx live
render(() => (
    <Container>
        <FilterTagDesktop
            size={40}
            shape='rectangular'
            checked={true}
            showArrow={true}
            showClear={true}
            leftAddons={
                <div
                    style={{
                        width: 16,
                        height: 16,
                        backgroundColor: 'var(--color-light-status-muted-alt-info)',
                        border: '1px dashed var(--color-light-status-info)',
                        borderRadius: '4px',
                    }}
                />
            }
        >
            <span>Label: Value</span>
        </FilterTagDesktop>
    </Container>
));
```
