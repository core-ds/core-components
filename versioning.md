---
marp: true
---

# 🙌 Немного о `@alfalab/core-components`

---

## 😎 Обзор текущего `@alfalab/core-components`

Из себя представляет:

-   источник истины дизайн системы 🤩
-   используется кучей проектов 📈
-   монорепозиторий 👨‍💻
-   более 100 атомарных `npm` пакетов ⚛️
-   `root` пакет, включающий все атомарные пакеты\* 💍

<br />
<br />
<br />
<br />

\* на самом деле не так, но об этом позже

---

## 🤯 О проблеме

Проблемы бывают разные, остановимся на 👉 **версионировании** 👈

---

## 🔎 Обзор текущего версионирования

В пакетах используется независимое версионирование, соответствующее `semver`\*:

-   `root` пакет `@alfalab/core-components@48.28.0`
-   типографика `@alfalab/core-components-typography@4.14.3`
-   и тд

<br />
<br />
<br />
<br />
<br />
<br />

_\* на самом деле не так, но об этом позже_

---

### 🛠️ `Patch`\* релиз при текущем версионировании

Допустим необходим `patch` типографики, соответственно, происходит релиз:

-   типографика `@alfalab/core-components-typography@4.14.4`
-   `root` пакет `@alfalab/core-components@48.28.1`
-   _длинный список пакетов, зависящих от типографики_ с инкрементом с `patch` версии (автоматически)

<br />
<br />
<br />
<br />
<br />
<br />

\***Тут все ок!**

---

### 🆕 `Feature`\* релиз при текущем версионировании

Допустим выходит `feature` типографики, соответственно, происходит релиз:

-   типографика `@alfalab/core-components-typography@4.15.0`
-   `root` пакет `@alfalab/core-components@48.29.0`
-   _длинный список пакетов, зависящих от типографики_ с инкрементом `feature` или `patch` версии (зависит от `changeset`)

<br />
<br />
<br />
<br />
<br />
<br />

\***Тут все ок!**

---

### 💥 `Major` релиз при текущем версионировании

Допустим выходит `major` типографики, соответственно, происходит релиз:

-   типографика `@alfalab/core-components-typography@5.0.0`
-   `root` пакет `@alfalab/core-components@49.0.0`
-   _длинный список пакетов, зависящих от типографики_ с инкрементом `{major,feature,patch}` версии\* (автоматически и зависит от `changeset`)

<br />
<br />
<br />
<br />
<br />
<br />

\*то самое "соответствие" `semver`

---

### ❓ Почему так?

Всему виной... люди 👫. Они ошибаются 😢

![bg fit right:50%](auf.jpg)

---

### 👨‍🔧 Исправим ошибку! Но как?

Только очевидные варианты:

-   не ошибайтесь (спасибо пожалуйста) 👍

![bg fit right:50%](pinguin.jpg)

---

### 🙏 Как не ошибаться и... поддерживать любую версию `@alfalab/core-components` и `@alfalab/core-components-*`?

А вот так:

-   ~~починить версионирование~~ 👎
-   ~~починить~~ версионирование 👍

...и исключить возможность ошибок версионирования в целом 😼

---

### 🔬 Способы версионирования пакетов

Их два с половиной:

-   мажорить все зависимые пакеты при мажорке пакета 💅 (Независимое версионирование)
-   мажорить все пакеты при мажорке пакета ✨ (общее версионирование)
-   фиксированно версионировать все пакеты 🧠 (фиксированное версионирование)

---

### 💅 Независимое версионирование

> Пакет к успеху шел, не получилось, не фартануло

По сути `@alfalab/core-components` и `@alfalab/core-components-*` так и должны версионироваться

➕:

-   семантичность (по `semver`)
-   независимость

➖:

-   требуется анализ зависимостей
-   ошибки приводят к нарушению `semver`
-   сложность поддержки предыдущих версий пакетов

---

### ✨ Общее версионирование

➕:

-   простота для разработчиков
-   удобная поддержка предыдущих версий пакетов

➖:

-   версии-пустышки, т.е. пакет обновится мажорно, не получив ничего
-   разные версии пакетов, например
    -   типографика `@alfalab/core-components-typography@4.14.3`
    -   селект `@alfalab/core-components-select@17.21.4`
    -   и тд

---

### 🧠 Фиксированное версионирование

➕:

-   простота и удобство, как для разработчиков так и для пользователей
-   удобная поддержка предыдущих версий пакетов
-   сематика говорящей версии, т.е. атомарная типографика `@alfalab/core-components-typography@49.7.2` соответствует типографике из `root` пакета `@alfalab/core-components@49.7.2`

➖:

-   версии-пустышки, т.е. пакет обновится, не получив ничего

---

### 🛍️ Немного про новый `root` пакет

С релизом нового `root` пакета, он станет пакетом пакетов (как тот на кухне):

```json
{
    "name": "@alfalab/core-components",
    "version": "49.0.0",
    "dependencies": {
        "@alfalab/core-components-accordion": "x.y.z",
        "@alfalab/core-components-action-button": "a.b.c"
    }
}
```

Сейчас:

⚛️ + ⚛️ = ❤️ и ⚛️ + 🛍️ = 💔

После релиза:

⚛️ + 🛍️ = ❤️ и ⚛️ + 🛍️ = ❤️

---

### 🤔 Почему бы всем не перейти на атомарные пакеты?

`root` пакет хоть и состоит из атомарных пакетов, имеет одно главное преимущество - обеспечение **констистентности** компонентов дизайн системы 🪄

Важно: **не решается** проблема версионирования. Дело не в `root` пакете 🤷‍♂️

---

### Given

Для примера используется зависящий от него `@alfalab/core-components-accordion`, независящего `@alfalab/core-components-card-image`, и само самой `root` пакет - `@alfalab/core-components`

| Независимое         | Общее               | Фиксированное       |
| ------------------- | ------------------- | ------------------- |
| `accordion@1.2.15`  | `accordion@1.2.15`  | `accordion@1.2.15`  |
| `card-image@3.2.3`  | `card-image@3.2.3`  | `card-image@3.2.3`  |
| `typography@4.14.4` | `typography@4.14.4` | `typography@4.14.4` |
| `root@48.28.0`      | `root@48.28.0`      | `root@48.28.0`      |

---

### When

Происходит мажорное обновление пакета `@alfalab/core-components-typography`

---

### Then

| Независимое        | Общее              | Фиксированное       |
| ------------------ | ------------------ | ------------------- |
| `accordion@2.0.0`  | `accordion@2.0.0`  | `accordion@49.0.0`  |
| `card-image@3.2.3` | `card-image@4.0.0` | `card-image@49.0.0` |
| `typography@5.0.0` | `typography@5.0.0` | `typography@49.0.0` |
| `root@49.0.0`      | `root@49.0.0`      | `root@49.0.0`       |

---

# 👏 Спасибо за внимание!
