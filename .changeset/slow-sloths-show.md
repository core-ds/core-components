---
'@alfalab/core-components': patch
'@alfalab/core-components-themes': patch
---

##### Themes

###### FormControl

- Из темизации `click` и `site` удалена переменная `--form-control-mobile-sub-paddings` которая переопределяла отступы для `outer label`, `hint`, `error`. Переопределяемое значение теперь совпадает с текущим значением компонента.

###### CheckboxGroup

- Из темизации `click` и `site` удалена переменная `--checkbox-group-mobile-sub-padding-left` которая переопределяла боковой отступ для `label` и `hint`. Переопределяемое значение теперь совпадает с текущим значением компонента.

###### RadioGroup

- Из темизации `click` и `site` удалена переменная `--radio-group-mobile-sub-padding-left` которая переопределяла боковой отступ для `label` и `hint`. Переопределяемое значение теперь совпадает с текущим значением компонента.
