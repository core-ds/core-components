---
'@alfalab/core-components-types': minor
'@alfalab/core-components-underlay': minor
---

- В пропс `shadow` (и `contentProps.shadow`) компонента `Underlay` добавлена поддержка теней `superapp-shadow-default` и `superapp-shadow-promo` (токены `--shadow-default` и `--shadow-promo` из набора `shadows-superapp.css`)
